// 15766
[proc,script15766](struct $struct0, int $int1)(int, string, int, string)
def_string $string0 = "1 skip token";
def_int $int2 = ~currency_pouch_total(dbrow_4971);
def_string $string1 = "";
if ($int2 = 0) {
    $string1 = "You do not have any challenge skip tokens in your currency pouch. ";
    $string1 = append($string1, " ");
    $string1 = append($string1, "You can get challenge skip tokens from daily challenge weekly progress chests.");
    return(0, $string1, $int2, $string0);
}
if (~script3841($int1) >= struct_param($struct0, param_2235) & ~script10583($int1) = 1) {
    return(0, "You've already completed this challenge.", $int2, $string0);
}
if (~script10580($int1) = 1) {
    if (~script15759($int1) = 0) {
        $string0 = "2 skip tokens";
        $string1 = "Spend two skip tokens to complete this extended challenge.";
    } else {
        $string1 = "Spend a skip token to complete the remainder of this extended challenge.";
    }
} else {
    $string1 = "Spend a skip token to complete this challenge.";
}
return(1, $string1, $int2, $string0);
