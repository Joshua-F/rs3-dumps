// 18808
[clientscript,script18808](int $int0)
if (cc_find(interface_1174:com_24, $int0) = true) {
    ~script7872(true, true, true, true);
}
def_dbrow $dbrow1 = null;
def_int $int2 = db_find_with_count(dbtable_279:col1, 1);
def_int $int3 = 0;
def_int $int4 = -1;
switch_int (%varplayerbit_55284) {
    case 1 :
        $int2 = db_find_with_count(dbtable_279:col1, 2);
    case 2 :
        $int2 = db_find_with_count(dbtable_280:col1, 1);
    case 3 :
        $int2 = db_find_with_count(dbtable_280:col1, 2);
}
while (++$int4 < $int2) {
    $dbrow1 = db_findnext;
    if ($dbrow1 ! null) {
        switch_int (%varplayerbit_55284) {
            case 0, 1 :
                $int3 = db_getfield($dbrow1, dbtable_279:col0, 0);
            case 2, 3 :
                $int3 = db_getfield($dbrow1, dbtable_280:col0, 0);
        }
        if (cc_find(interface_1174:com_24, $int3) = true & $int3 ! $int0) {
            ~script7872(true, true, true, false);
        }
    }
}
~script18809($int0);
