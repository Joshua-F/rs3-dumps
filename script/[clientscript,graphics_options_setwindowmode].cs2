// 2698
[clientscript,graphics_options_setwindowmode](int $int0, int $int1, int $int2, int $int3, int $int4)
def_int $int5 = detailget_maxscreensize;
def_int $int6 = 0;
if ($int0 = 1) {
    $int6 = 2;
} else if ($int0 = 2) {
    $int6 = 0;
} else {
    $int6 = $int5;
}
if ($int0 = getwindowmode & $int6 = $int5) {
    return;
}
if (autosetup_getlevel ! 0) {
    autosetup_setcustom;
}
def_int $int7 = -1;
if ($int0 ! 3) {
    $int0 = $int6;
    setwindowmode(^windowmode_resizable);
    setdefaultwindowmode(2);
    if (%varclient_4108 ! 5 & %varclient_4108 ! -1) {
        ~script709(%varclient_4108, $int0);
        ~script8885(%varclient_4108);
    } else {
        detail_maxscreensize($int0);
    }
    $int7 = detailget_maxscreensize;
    if ($int0 ! $int7) {
        ~graphics_options_message($int4, 0, "RuneScape was unable to enter that display mode.<br><br>Please visit our website for more information.", "", "");
        ~graphics_options_rebuild($int3, $int7, $int1, $int2, $int4);
        return;
    }
    ~graphics_options_rebuild($int3, $int7, $int1, $int2, $int4);
    if ($int7 = 2) {
        %varclient_994 = 2;
    }
    autosetup_setcustom;
    return;
}
if (playermember = false) {
    ~graphics_options_message($int4, 0, "Fullscreen mode is only available to RuneScape members.<br><br>Please visit the RuneScape website to learn about other benefits of membership.", "Members' benefits", "members_benefits.ws");
    return;
}
if (fullscreen_modecount > %varclient_178 & fullscreen_enter(fullscreen_getmode(%varclient_178)) = true) {
    if ($int4 = 4) {
        ~script7932;
    }
    ~graphics_options_rebuild($int3, $int0, $int1, $int2, $int4);
    ~script2700(2, $int4, 0, 0);
    return;
}
if ($int4 = 1) {
    mes("Unable to enter fullscreen mode at that resolution.");
}
~graphics_options_rebuild($int3, $int0, $int1, $int2, $int4);
