// 12061
[proc,invent_blueprint_check_requirements](dbrow $dbrow0)(int)
def_int $int1 = db_getfield($dbrow0, dbtable_5:col7, 0);
def_int $int2 = ~script12047($dbrow0);
if ($int2 < 0) {
    return(0);
}
$int1 = calc($int1 + $int2);
if (stat_base(invention) < $int1 | stat(invention) < $int1) {
    return(0);
}
def_stat $stat3 = null;
def_int $int4 = 0;
if (%varplayerbit_30223 < 105) {
    switch_dbrow ($dbrow0) {
        case dbrow_205 :
            if (%varplayerbit_30223 < 5 & %varplayerbit_30223 < 35 & %varplayerbit_30223 >= 30) {
                return(0);
            }
        case dbrow_206 :
            if (%varplayerbit_30223 < 55) {
                return(0);
            }
        case dbrow_207 :
            if (%varplayerbit_30223 < 80) {
                return(0);
            }
        case default :
            return(0);
    }
}
def_int $int5 = db_getfieldcount($dbrow0, dbtable_5:col9);
while ($int5-- > 0) {
    $stat3, $int4 = db_getfield($dbrow0, dbtable_5:col9, $int5);
    if (stat($stat3) < $int4) {
        return(0);
    }
}
$int5 = db_getfieldcount($dbrow0, dbtable_5:col8);
while ($int5-- > 0) {
    $int4 = db_getfield($dbrow0, dbtable_5:col8, $int5);
    if (~ql4_getprogress($int4) ! 2) {
        return(0);
    }
}
$int5 = db_getfieldcount($dbrow0, dbtable_5:col10);
while ($int5-- > 0) {
    $int4 = db_getfield($dbrow0, dbtable_5:col10, $int5);
    if (~invent_blueprint_unlocked($int4) ! 1) {
        return(0);
    }
}
return(1);
