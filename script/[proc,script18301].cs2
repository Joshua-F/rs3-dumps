// 18301
[proc,script18301](int $int0)(graphic)
db_find(dbtable_263:col3, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Attempting to get graphic for an emote with invalid id = <tostring($int0)>.");
    return(null);
}
return(db_getfield($dbrow1, dbtable_263:col1, 0));
