// 1999
[clientscript,script1999](component $component0, boolean $boolean1, int $int2, int $int3, int $int4, int $int5, int $int6, int $int7)
def_int $int8 = %varplayer_1749;
def_int $int9 = %varplayer_1750;
def_int $int10 = %varplayer_1751;
def_int $int11 = %varplayer_1752;
if ($int2 = 0 & ($int9 ! 0 | $int10 ! 0 | $int11 ! 0 | %varplayer_1749 ! 0)) {
    $int2 = 1;
}
if ($int2 = 1) {
    if (%varclient_1745 = true) {
        if (video_advert_has_finished = false) {
        }
        if (video_advert_has_finished = true) {
            ~lobbyscreen_load($boolean1);
            if_setontimer(null, $component0);
            detail_soundvol($int3);
            detail_bgsoundvol($int5);
            detail_speechvol($int6);
            detail_loginvol($int7);
            %varclient_1745 = false;
            return;
        }
    } else {
        if (playermember = false & $int10 > 0 & $int10 < $int11) {
            if (video_advert_play(5) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
            return;
        }
        if (date_runeday <= calc($int8 + 30)) {
            if (video_advert_play(1) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else if (date_runeday <= calc($int8 + 182)) {
            if (video_advert_play(2) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else if (date_runeday <= calc($int8 + 365)) {
            if (video_advert_play(3) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else {
            if (video_advert_play(4) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        }
    }
}
