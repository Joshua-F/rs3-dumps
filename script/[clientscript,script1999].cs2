// 1999
[clientscript,script1999](component $component0, int $int1, int $int2, int $int3, int $int4, int $int5, int $int6)
def_int $int7 = %varplayer_1749;
def_int $int8 = %varplayer_1750;
def_int $int9 = %varplayer_1751;
def_int $int10 = %varplayer_1752;
if ($int1 = 0 & ($int8 ! 0 | $int9 ! 0 | $int10 ! 0 | %varplayer_1749 ! 0)) {
    $int1 = 1;
}
if ($int1 = 1) {
    if (%varclient_1745 = true) {
        if (video_advert_has_finished = false) {
        }
        if (video_advert_has_finished = true) {
            ~lobbyscreen_load;
            if_setontimer(null, $component0);
            clientoption_set(^clientoption_volume_main_effects, $int2);
            clientoption_set(^clientoption_volume_background_effects, $int4);
            clientoption_set(^clientoption_volume_speech, $int5);
            clientoption_set(^clientoption_volume_login_music, $int6);
            %varclient_1745 = false;
            return;
        }
    } else {
        if (playermember = false & $int9 > 0 & $int9 < $int10) {
            if (video_advert_play(5) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
            return;
        }
        if (date_runeday <= calc($int7 + 30)) {
            if (video_advert_play(1) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else if (date_runeday <= calc($int7 + 182)) {
            if (video_advert_play(2) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else if (date_runeday <= calc($int7 + 365)) {
            if (video_advert_play(3) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        } else {
            if (video_advert_play(4) = true) {
            }
            %varclient_1745 = true;
            ~lobbyscreen_mute_volume;
        }
    }
}
