// 437
[proc,script437](dbrow $dbrow0, int $int1, int $int2)(int)
if ($dbrow0 = null) {
    ~error("$data was null, returning null.");
    return(-1);
}
if (db_getrowtable($dbrow0) ! dbtable_35) {
    ~error("$data was not of dbtable probability_distribution, returning null.");
    return(-1);
}
if (db_getfieldcount($dbrow0, dbtable_35:col0) = 0) {
    ~error("The non-null dbrow we are using has no outcomes, returning null.");
    return(-1);
}
def_int $int3 = db_getfieldcount($dbrow0, dbtable_35:col0);
def_int $intarray0($int3);
def_int $intarray1($int3);
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = -1;
def_int $int7 = -1;
while (++$int7 < $int3) {
    $int5, $int6 = db_getfield($dbrow0, dbtable_35:col0, $int7);
    if ($int5 ! $int1 & $int5 ! $int2) {
        $int4 = calc($int4 + $int6);
        $intarray0($int7) = $int4;
        $intarray1($int7) = $int5;
    }
}
if ($int4 = 0) {
    ~error("We found a dbrow defined for the distribtuion but it had no probability data, returning null.");
    return(-1);
}
def_int $int8 = random($int4);
$int7 = -1;
while (++$int7 < $int3) {
    if ($int8 < $intarray0($int7)) {
        return($intarray1($int7));
    }
}
~error("Unable to find outcome for dbrow, returning null");
return(-1);
