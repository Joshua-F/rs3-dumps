// 9784
[proc,script9784](int $int0)(string, string, boolean)
def_int $int1 = ~script13935($int0);
def_string $string0 = "Beast of Burden";
if ($int0 ! 11) {
    $string0 = ~script13936($int1);
}
def_string $string1 = tostring($int0);
def_string $string2 = "<$string0><br><br>Load this preset and close the bank interface.";
def_boolean $boolean2 = false;
switch_int ($int0) {
    case 3, 4, 5 :
        if (playermember = false) {
            $string2 = "This slot is only available to members.";
            $boolean2 = true;
        }
    case 6 :
        if (%varplayerbit_22153 = 0 & %varplayer_4292 < date_minutes) {
            $string2 = "This slot can be unlocked temporarily with Loyalty Points or permanently with RuneCoins.";
            $boolean2 = true;
        }
    case 7 :
        if (%varplayerbit_22154 = 0 & %varplayer_4293 < date_minutes) {
            $string2 = "This slot can be unlocked temporarily with Loyalty Points or permanently with RuneCoins.";
            $boolean2 = true;
        }
    case 8 :
        if (%varplayerbit_22155 = 0 & %varplayer_4294 < date_minutes) {
            $string2 = "This slot can be unlocked temporarily with Loyalty Points or permanently with RuneCoins.";
            $boolean2 = true;
        }
    case 9 :
        if (%varplayerbit_22156 = 0 & %varplayer_4295 < date_minutes) {
            $string2 = "This slot can be unlocked temporarily with Loyalty Points or permanently with RuneCoins.";
            $boolean2 = true;
        }
    case 10 :
        if (%varplayerbit_22157 = 0 & %varplayer_4296 < date_minutes) {
            $string2 = "This slot can be unlocked temporarily with Loyalty Points or permanently with RuneCoins.";
            $boolean2 = true;
        }
    case 11 :
        $string1 = "B";
        if (playermember = false) {
            $string2 = "A beast of burden preset can only be loaded on members' worlds.";
            $boolean2 = true;
        }
}
return($string1, $string2, $boolean2);
