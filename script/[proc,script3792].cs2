// 3792
[proc,script3792](inv $inv0, obj $obj1, int $int2)(int)
def_int $int3 = 0;
if (~script3802($obj1) = false) {
    if (inv_total($inv0, $obj1) > 0) {
        $int3 = max(0, calc($int2 - (^max_32bit_int - inv_total($inv0, $obj1))));
    } else if (inv_freespace($inv0) = 0) {
        $int3 = $int2;
    }
} else {
    $int3 = max(0, calc($int2 - inv_freespace($inv0)));
}
return($int3);
