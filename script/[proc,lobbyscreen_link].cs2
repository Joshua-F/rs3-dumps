// 3838
[proc,lobbyscreen_link](string $string0, string $string1, boolean $boolean0)
if (compare($string0, "mobile") = 0 & compare($string1, "mobile") = 0) {
    if (~is_mobile_client = true) {
        ~mobile_open_shop(4);
    }
    return;
}
if (compare($string0, "dob") = 0 & compare($string1, "set_members_dob.ws") = 0) {
    if_sethide(false, interface_906:com_263);
    if (~is_mobile_client = true) {
        ~mobile_open_shop(4);
    } else {
        openurl_shim("billing_core", "purchasepopup.ws?externalName=rs", "packagegroupredirect.ws?value=rs", true);
        %varclient_1795 = clientclock;
        if_setontimer("script6032(0)", interface_906:com_598);
    }
} else if (compare($string0, "") ! 0) {
    openurl($string0, $string1, $boolean0);
}
