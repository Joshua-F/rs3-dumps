// 2697
[clientscript,script2697](int $int0, int $int1)
if ($int0 = detailget_toolkit) {
    return;
}
detail_toolkit($int0);
def_int $int2 = detailget_toolkit;
if ($int0 ! $int2) {
    ~graphics_options_message($int1, "Unable to enter that mode.");
}
~script2593($int2);
~graphics_options_rebuild($int2, getwindowmode, ~graphics_options_reviewoptions($int2), $int1);
if ($int2 > 0) {
    ~script2700(1, $int1);
} else {
    detail_toolkit_default($int2);
}
