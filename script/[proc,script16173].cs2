// 16173
[proc,script16173](struct $struct0)(int)
def_dbrow $dbrow1 = ~script9304($struct0);
if ($dbrow1 = null) {
    ~error("We are trying to bulid the TRH prizes but got a null $promo_data dbrow, falling back to default table.");
    $dbrow1 = dbrow_4562;
}
if (db_getfieldcount($dbrow1, dbtable_147:col0) = 0) {
    ~error("The non-null dbrow we are using to generate TRH prizes has no categories specified on it, falling back to the default table.");
    $dbrow1 = dbrow_4562;
}
def_int $int2 = db_getfieldcount($dbrow1, dbtable_147:col0);
def_int $int3 = 0;
def_int $intarray0(max($int2, db_getfieldcount(dbrow_4562, dbtable_147:col0)));
def_int $intarray1(max($int2, db_getfieldcount(dbrow_4562, dbtable_147:col0)));
def_int $int4 = 0;
def_dbrow $dbrow5 = null;
def_int $int6 = 0;
def_int $int7 = -1;
while (++$int7 < $int2) {
    $int3, $dbrow5, $int6 = db_getfield($dbrow1, dbtable_147:col0, $int7);
    switch_int ($int3) {
        case 1, 2, 4, 5, 6, 7 :
            $int4 = calc($int4 + ~script16165($int3, $dbrow1));
    }
    $intarray0($int7) = $int4;
    $intarray1($int7) = $int3;
}
if ($int4 = 0) {
    ~error("We found a dbrow defined for the current promo but it had no prize data, so are now using the default drop table.");
    $dbrow1 = dbrow_4562;
    $int3 = 0;
    $int7 = -1;
    $int2 = db_getfieldcount($dbrow1, dbtable_147:col0);
    while (++$int7 < $int2) {
        $int3, $dbrow5, $int6 = db_getfield($dbrow1, dbtable_147:col0, $int7);
        switch_int ($int3) {
            case 1, 2, 4, 5, 6, 7 :
                $int4 = calc($int4 + ~script16165($int3, $dbrow1));
        }
        $intarray0($int7) = $int4;
        $intarray1($int7) = $int3;
    }
}
def_int $int8 = random($int4);
$int7 = -1;
while (++$int7 < $int2) {
    if ($int8 < $intarray0($int7)) {
        return($intarray1($int7));
    }
}
~error("we were unable to roll a category into a player's slot, going to the specified fallback of: <tostring(db_getfield($dbrow1, dbtable_147:col4, 0))>");
return(db_getfield($dbrow1, dbtable_147:col4, 0));
