// 7855
[proc,create_validate_displayname](int $int0, int $int1)
%varclient_3685 = %varclient_2563;
if_settext("<tostring(string_length(%varclient_2563))> / 12", interface_786:com_9);
if (string_length(%varclient_2563) <= 0) {
    ~script7845(5, "Display names can be up to 12 characters long and may contain letters, numbers, spaces, underscores and dashes only.");
    return;
}
if (string_indexof_string("- _", substring(%varclient_2563, 0, 1), 0) ! -1) {
    ~create_error("Display names cannot start with a space, dash or underscore.");
    return;
}
if (~create_check_displayname(%varclient_2563) = false) {
    ~create_error("Please enter a valid display name.");
    return;
}
if (if_gettop = interface_906) {
    ~script7845(4, "You have entered a valid character name!<br>Please wait while the system checks availability.");
    create_name_availablerequest(%varclient_2563);
    if_setontimer("create_validate_displayname_timer", interface_1420:com_7);
} else {
    if_triggerop(interface_786:com_0, -1, 1);
    if_sethide(true, interface_786:com_10);
    if_sethide(false, interface_786:com_11);
}
