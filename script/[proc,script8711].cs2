// 8711
[proc,script8711](int $int0, int $int1, int $int2)(int)
def_int $int3 = 10;
switch_int ($int0) {
    case 0 :
        return(0);
    case 2048 :
        return(calc($int2 / 2 - $int1 / 2));
    case 4095 :
        return(calc($int2 - $int1));
    case default :
        $int0 = calc($int0 * $int3);
        $int1 = calc($int1 * $int3);
        $int2 = calc($int2 * $int3);
        $int0 = interpolate(calc((0 - $int2) / 2), calc($int2 / 2), calc(0 * $int3), calc(4095 * $int3), $int0);
        $int0 = calc($int0 - $int1 / 2 + $int2 / 2);
        $int0 = max(0, min($int0, calc($int2 - $int1)));
        return(calc($int0 / $int3));
}
