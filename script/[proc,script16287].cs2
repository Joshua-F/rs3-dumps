// 16287
[proc,script16287](int $int0)
~mtx_benefits_draw_title($int0, "Additional Items");
def_string $string0 = "";
def_int $int1 = -1;
def_string $string1 = "";
$int1 = ~script11191(category_5096);
if ($int1 ! -1 | %varplayerbit_50563 > 0 | ~script15757 = false & ~script11568 = true) {
    ~script16267($int0, "Premier Tokens");
}
if (%varplayerbit_50563 > 0) {
    if (%varplayerbit_50563 = 1) {
        $string1 = "You've got 1 more Premier reward token to claim.";
    } else {
        $string1 = "You've got <tostring(%varplayerbit_50563)> more Premier reward tokens to claim.";
    }
    ~script16269($int0, $string1, "Claim Premier Reward Token", 9);
}
if ($int1 ! -1) {
    $string0 = ~script15458($int1);
    if (string_length($string0) > 0) {
        $string1 = "You've got some left-over Premier Reward Tokens tucked away in your <col=FFFFFF><$string0></col>. You can use them to claim various cosmetic rewards.";
    } else {
        $string1 = "You've got some left-over Premier Reward Tokens. You can use them to claim various cosmetic rewards.";
    }
    ~script16268($int0, $string1);
}
if (~script15757 = false) {
    ~script16292($int0, "12-month members get three Premier Reward tokens, allowing them access to three cosmetic items from past events!");
}
