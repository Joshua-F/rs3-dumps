// 6298
[proc,script6298]()(string)
def_int $int0 = ~script6304;
def_int $int1 = random($int0);
def_int $int2 = 0;
def_int $int3 = 0;
while ($int3 <= $int1) {
    if (string_length(~script6302($int2)) > 0) {
        if ($int3 = $int1) {
            return(~script6302($int2));
        }
        $int3 = calc($int3 + 1);
    }
    $int2 = calc($int2 + 1);
}
return("When the “earn free spin” button appears on your claim screen, spare a moment to watch a short video ad for a free spin!");
