// 943
[clientscript,script943](int $int0, int $int1, int $int2, int $int3)
def_enum $enum4 = struct_param(enum(int, struct, enum_169, $int0), param_782);
if (map_members = false) {
    mes("More advanced grouping options are available on a members' world.");
} else if (%varplayer_1295 < 1000) {
    mes("More advanced grouping options will be available when you've finished the Tutorial.");
} else {
    if (%varclient_693 = $int1) {
        %varclient_694 = calc((1 + %varclient_694) % 2);
    } else {
        %varclient_693 = $int1;
        %varclient_694 = 0;
    }
    ~ql4_sort($int0, %varclient_693, %varclient_694, %varclient_692, %varclient_1103);
}
