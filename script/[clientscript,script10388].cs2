// 10388
[clientscript,script10388]
if (clienttype = enhanced_ios) {
    ~script426("Push notifications are not currently available on iOS", null, -1, -1);
    return;
}
notifications_opensettings;
