// 7421
[proc,script7421](int $int0)(boolean)
if ($int0 = 0 | $int0 > 300) {
    return(false);
}
def_int $int1 = 0;
def_string $string0 = "";
def_int $int2 = 0;
def_string $string1 = "";
def_int $int3 = 0;
def_int $int4 = 0;
def_string $string2 = "";
$int1, $string0, $int2, $string1, $int3, $int4, $string2 = worldlist_specific($int0);
if (testbit($int1, 12) = ^true) {
    return(false);
}
if (testbit($int1, 30) = ^true & ~script16823 = false & staffmodlevel < 2) {
    return(false);
} else if (testbit($int1, 30) = ^false & ~script16823 = true & staffmodlevel < 2) {
    return(false);
}
switch_int (%varplayerbit_26925) {
    case 1 :
        if (testbit($int1, 0) = ^false) {
            return(false);
        }
    case 2 :
        if (testbit($int1, 0) = ^true) {
            return(false);
        }
}
switch_int (%varplayerbit_26926) {
    case 1 :
        if (testbit($int1, 3) = ^false) {
            return(false);
        }
    case 2 :
        if (testbit($int1, 3) = ^true) {
            return(false);
        }
}
return(true);
