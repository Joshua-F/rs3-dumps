// 20859
[clientscript,script20859]
if (~script20866 = true) {
    ~script20858;
    return;
}
%beta_login_imported_client = ~negate_bool(%beta_login_imported_client);
if (cc_find(marketplace_store:categories_layer, 1000) = true) {
    ~script7889(true, true, %beta_login_imported_client);
}
if (%marketplace_store_side_tab_id ! 1) {
    if_setscrollpos(0, 0, marketplace_store:items_holder);
    ~script20630(%marketplace_store_side_tab_id);
}
