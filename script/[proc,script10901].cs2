// 10901
[proc,script10901](int $int0, int $int1, int $int2)(string)
def_string $string0 = "";
if ($int0 < 10) {
    $string0 = "0<tostring($int0)>";
} else {
    $string0 = tostring($int0);
}
def_string $string1 = "";
if ($int1 < 10) {
    $string1 = "0<tostring($int1)>";
} else {
    $string1 = tostring($int1);
}
def_string $string2 = "";
switch_int (map_lang) {
    case default :
        if ($int2 = 1) {
            $string2 = append($string0, append(":", $string1));
        } else {
            $string2 = append($string0, append(" ", $string1));
        }
    case 1 :
        if ($int2 = 1) {
            $string2 = append($string0, append(":", $string1));
        } else {
            $string2 = append($string0, append(" ", $string1));
        }
    case 2 :
        if ($int2 = 1) {
            $string2 = append($string0, append(":", $string1));
        } else {
            $string2 = append($string0, append(" ", $string1));
        }
    case 3 :
        if ($int2 = 1) {
            $string2 = append($string0, append(":", $string1));
        } else {
            $string2 = append($string0, append(" ", $string1));
        }
    case 6 :
        if ($int2 = 1) {
            $string2 = append($string0, append(":", $string1));
        } else {
            $string2 = append($string0, append(" ", $string1));
        }
}
return($string2);
