// 8885
[proc,script8885](int $int0)
def_int $int1 = 0;
$int1 = ~script2257($int0);
def_int $int2 = 0;
def_int $int3 = 0;
if (getwindowmode = ^windowmode_small) {
    setwindowmode(^windowmode_resizable);
    setdefaultwindowmode(2);
}
if (detailget_maxscreensize ! $int1 & detailcanmod_maxscreensize = true & detailcanset_maxscreensize($int1) = 1) {
    detail_maxscreensize($int1);
}
if (%varclient_4108 = 8) {
    ~script8702($int0, 8);
} else {
    ~script8703($int0, %varclient_4108);
}
