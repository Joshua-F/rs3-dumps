// 7661
[clientscript,script7661](component $component0, int $int1, struct $struct2, int $int3, string $string0)
def_string $string1 = "";
def_string $string2 = "";
if ($int3 = -1) {
    $string2 = "Requirements:";
    $string1 = ~cheevo_requirements_response($struct2);
    $string1 = ~cheevo_quest_tooltip($string1, $struct2, $string0);
    if (compare($string1, "") ! 0) {
        $string2 = append($string2, "<br><$string1>");
        $string2 = ~script13616($string2, $struct2);
        if (compare($string0, "") ! 0) {
            $string2 = append($string2, "<br><br><$string0>");
        }
    } else if (compare($string0, "") ! 0) {
        $string2 = ~script13616($string2, $struct2);
        $string2 = append($string2, "<br><$string0>");
    } else {
        $string2 = "";
    }
} else {
    $string2 = ~cheevo_quest_tooltip($string2, $struct2, $string0);
    if (compare(struct_param($struct2, param_1274), "") ! 0 | compare(struct_param($struct2, param_2225), "") ! 0) {
        $string2 = ~script13616($string2, $struct2);
        $string2 = append($string2, "<br><br><$string0>");
    } else if (compare($string0, "") ! 0) {
        $string2 = append($string2, "<br><$string0>");
    }
}
if (cc_find($component0, $int1) = true) {
    if (compare($string2, "") ! 0) {
        cc_setonmouserepeat("script3876($string2, event_com, event_comsubid)");
    } else {
        cc_setonmouserepeat(null);
    }
}
