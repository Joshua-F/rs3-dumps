// 18989
[proc,script18989]()(int)
if (~script18991 = true) {
    if (%varplayerbit_55644 = 7) {
        return(1);
    } else {
        ~error("24/25 bits set, but cat13 is not unlocked, state: <tostring(%varplayerbit_55644)>, correcting");
        ~script19001;
        return(1);
    }
} else if (%varplayerbit_55644 = 7) {
    ~error("<tostring(bitcount(getbit_range(%varplayer_11751, 0, 24)))>/25 bits sets, but cat13 is UNLOCKED");
}
return(0);
