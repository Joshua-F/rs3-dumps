// 11837
[proc,script11837](int $int0, int $int1)(int)
def_int $int2 = 0;
if ($int1 < 1) {
    return($int0);
}
if ($int1 < 60) {
    $int2 = calc(scale(60, $int1, 100) * $int0 / 100);
} else {
    $int2 = calc($int0 * 100 / scale($int1, 60, 100));
}
return($int2);
