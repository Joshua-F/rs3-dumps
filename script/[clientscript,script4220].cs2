// 4220
[clientscript,script4220]
~script6575;
def_int $int0 = -1;
def_int $int1 = 0;
if (cc_find(interface_755:com_85, 0) = true) {
    ~script7909(true, enum_17000, -1, %varplayerbit_54002);
    $int0 = enum_getoutputcount(enum_3003);
    if ($int0 ! enum_getoutputcount(enum_17000)) {
        ~error("Couldn't set area task dropdown red dot notifications in interface area_task_sub. Enums area_tasks_area_names and area_tasks_area_intern_notes_states are out of synch!");
        return;
    }
    while ($int1 < $int0) {
        if (var_reference_get(enum(int, var_int, enum_3003, $int1)) = 1) {
            cc_list_setentryicon($int1, icon_notification);
        } else {
            cc_list_setentryicon($int1, null);
        }
        $int1 = calc($int1 + 1);
    }
}
