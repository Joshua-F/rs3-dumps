// 6764
[proc,challenge_reqs_combat](int $int0, int $int1)(int)
if (comlevel_active < struct_param(enum(int, struct, enum_5513, $int0), param_2236)) {
    return(0);
}
if (map_members = false & $int1 = 0) {
    switch_int ($int0) {
        case 6, 10, 11, 12, 13, 16, 19, 22 :
            return(0);
        case default :
            return(1);
    }
}
switch_int ($int0) {
    case 6 :
        if (~ironman_mode = true) {
            return(0);
        }
    case 10 :
        if (stat_base(strength) < 60 & stat_base(agility) < 60 | comlevel_active < 130) {
            return(0);
        }
    case 11 :
        if (stat_base(strength) < 60 & stat_base(agility) < 60 | comlevel_active < 130) {
            return(0);
        }
    case 12 :
        if (stat_base(strength) < 60 & stat_base(agility) < 60 | comlevel_active < 130) {
            return(0);
        }
    case 13 :
        if (stat_base(strength) < 60 & stat_base(agility) < 60 | comlevel_active < 130) {
            return(0);
        }
    case 19 :
        if (stat_base(strength) < 70 & stat_base(agility) < 70 & stat_base(ranged) < 70 & stat_base(hitpoints) < 70 | comlevel_active < 140) {
            return(0);
        }
    case default :
        return(1);
}
return(1);
