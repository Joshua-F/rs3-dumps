// 1969
[proc,script1969](int $int0)
def_int $int1 = 1;
def_int $int2 = 0;
while ($int1 <= enum_getoutputcount(enum_2119)) {
    if (%varclient_617 = 1) {
        $int2 = enum(int, int, enum_2022, $int1);
        if_settext("<tostring($int2)> coins", enum(int, component, enum_2119, $int1));
    } else {
        $int2 = enum(int, int, enum_2021, $int1);
        if_settext("<tostring($int2)> coins", enum(int, component, enum_2119, $int1));
    }
    if (inv_total(inv_93, enum(int, namedobj, enum_2020, $int1)) < 1) {
        if_setcolour(^red, enum(int, component, enum_2119, $int1));
    } else if ($int0 >= $int2) {
        if_setcolour(^green, enum(int, component, enum_2119, $int1));
    } else {
        if_setcolour(^red, enum(int, component, enum_2119, $int1));
    }
    $int1 = calc($int1 + 1);
}
