// 6953
[proc,script6953]()(dbrow)
def_int $int0 = 2;
db_find(dbtable_288:col0, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Promotion skin is null when getting fighter. Using default.");
    $dbrow1 = dbrow_13839;
}
return($dbrow1);
