// 9220
[proc,script9220](inv $inv0, int $int1, obj $obj2, int $int3, int $int4, int $int5, component $component6, component $component7)
if ($inv0 ! inv_93 & $inv0 ! inv_95) {
    return;
}
if (~script9224($obj2, null) = 1) {
    return;
}
if (inv_freespace($inv0) = 0 & (oc_stackable($obj2) = false | inv_total($inv0, $obj2) > 0 & calc(^max_32bit_int - inv_getnum(inv_94, $int3)) < inv_getnum($inv0, ~script9250($inv0, $obj2)))) {
    if ($int1 = 1) {
        mes("You don't have enough space in your inventory to do that.");
    }
    return;
}
if ($inv0 = inv_93) {
    if ($int4 = -1) {
        ~script9215($int3, ~script9249(inv_93), $component6, $component7);
    } else if ($int4 >= 0 & $int4 < inv_size($inv0)) {
        if (inv_getobj($inv0, $int4) = null) {
            ~script9215($int3, $int4, $component6, $component7);
        }
    } else {
        return;
    }
} else if ($inv0 = inv_95) {
    if ($int4 ! -1) {
        ~script3807($obj2, inv_getnum(inv_94, $int3), $int4, inv_94, $int3, 1, 1, ~script3784, $component7, $component6);
    } else {
        ~script3805($obj2, inv_getnum(inv_94, $int3), inv_94, $int3, 1, 1, $component7, $component6);
    }
    return;
}
