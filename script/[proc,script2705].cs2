// 2705
[proc,script2705](int $int0, int $int1, int $int2, int $int3, int $int4, int $int5)
if ($int2 = 1) {
    if_closesubclient(interface_742:com_6);
    if_setontimer(null, interface_742:com_6);
} else if ($int2 = 2) {
    if_setontimer(null, interface_906:com_13);
} else if ($int2 = 4) {
    if_setontimer(null, interface_1420:com_555);
} else {
    if_setontimer(null, interface_744:com_24);
}
def_int $int6 = 0;
if ($int0 = 1) {
    detail_toolkit_default(detailget_toolkit, true);
    if (getwindowmode ! ^windowmode_fullscreen) {
        setdefaultwindowmode(getwindowmode);
    }
    detail_antialiasing_default(detailget_antialiasing);
    if (testbit($int1, 1) = ^true) {
        %varclient_994 = 2;
    }
    %varclient_1240 = 4;
    %varclient_1277 = false;
} else {
    if (testbit($int1, 0) = ^true) {
        autosetup_blackflaglast;
        if ($int4 = 0) {
            $int6 = detailget_toolkit_default;
            detail_toolkit($int6);
            ~script2593($int6, null);
        }
    } else {
        $int6 = detailget_toolkit;
    }
    if (testbit($int1, 1) = ^true & getwindowmode ! getdefaultwindowmode) {
        setwindowmode(getdefaultwindowmode);
    }
    if (testbit($int1, 2) = ^true) {
        detail_antialiasing(detailget_antialiasing_default);
    }
    if (testbit($int1, 3) = ^true) {
        detail_bloom(0);
    }
    if ($int2 = 3) {
        ~loginscreen_setactivemenu(0);
        ~autosetup($int2);
        return;
    }
    if ($int5 = 1) {
        ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
    } else if (testbit($int1, 1) = ^true | $int4 = 1) {
        ~graphics_options_rebuild($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
    } else {
        ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
    }
    if ($int2 = 1 & $int3 = 1) {
        mes("The requested change has been cancelled.");
    }
}
if ($int2 ! 1) {
    $int6 = detailget_toolkit;
    if ($int2 = 2) {
        if_sethide(true, interface_906:com_49);
        if ($int5 = 1) {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else if (testbit($int1, 1) = ^true | $int4 = 1) {
            ~graphics_options_rebuild($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        }
        if ($int4 = 0 & $int3 = 1) {
            ~lobbyscreen_input_full("", "The change of detail mode has been cancelled.<br><br>Perhaps different graphical settings would work better for you?", 0, -1, "", "", 0);
        }
    } else if ($int2 = 3) {
        if (has_signonkey = true) {
            ~loginscreen_setactivemenu(5);
            if (%varclient_1273 = true) {
                return;
            } else {
                if_setontimer("script3381(interface_975:com_21, 0)", interface_975:com_21);
            }
        } else if (%varclient_3684 ! 1 & ~userflowflags(3) = true) {
            ~loginscreen_setactivemenu(7);
        } else {
            %varclient_3684 = 0;
            ~loginscreen_setactivemenu(11);
        }
    } else if ($int2 = 4) {
        if ($int5 = 1) {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else if (testbit($int1, 1) = ^true | $int4 = 1) {
            ~graphics_options_rebuild($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        }
        if_sethide(true, interface_1420:com_556);
        if_sethide(false, interface_1420:com_425);
    } else {
        if ($int5 = 1) {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else if (testbit($int1, 1) = ^true | $int4 = 1) {
            ~graphics_options_rebuild($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        } else {
            ~script3387($int6, getwindowmode, ~graphics_options_reviewoptions($int6), $int2);
        }
        if ($int4 = 0 & $int3 = 1) {
            if_settext("The change of detail mode has been cancelled.<br><br>Perhaps different graphical settings would work better for you?", interface_744:com_104);
            if ($int5 = 1) {
                if_setonclick("loginscreen_setactivemenu_full(6, 0, 1)", interface_744:com_107);
            } else if (testbit($int1, 1) = ^true | $int4 = 1) {
                if_setonclick("loginscreen_setactivemenu(6)", interface_744:com_107);
            } else {
                if_setonclick("loginscreen_setactivemenu_full(6, 0, 1)", interface_744:com_107);
            }
            ~loginscreen_setactivemenu(9);
        } else if ($int5 = 1) {
            ~loginscreen_setactivemenu_full(6, 0, 1);
        } else if (testbit($int1, 1) = ^true | $int4 = 1) {
            ~loginscreen_setactivemenu(6);
        } else {
            ~loginscreen_setactivemenu_full(6, 0, 1);
        }
    }
}
if ($int0 = 0 & $int4 = 1) {
    ~autosetup($int2);
}
