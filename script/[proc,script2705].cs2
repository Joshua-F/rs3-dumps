// 2705
[proc,script2705](int $int0, int $int1, int $int2, int $int3, int $int4)
if ($int2 = 1) {
    if_closesubclient(interface_742:com_2);
    if_setontimer(null, interface_742:com_2);
} else if ($int2 = 2) {
    if_setontimer(null, interface_906:com_13);
} else if ($int2 = 4) {
    if_setontimer(null, interface_1420:com_555);
} else {
    if_setontimer(null, interface_744:com_0);
}
def_int $int5 = 0;
if ($int0 = 1) {
    detail_toolkit_default(detailget_toolkit, true);
    if (getwindowmode ! ^windowmode_fullscreen) {
        setdefaultwindowmode(getwindowmode);
    }
    detail_antialiasing_default(detailget_antialiasing);
    if (testbit($int1, 1) = ^true) {
        %varclient_994 = 2;
    }
    %varclient_1240 = 4;
    %varclient_1277 = false;
} else {
    if (testbit($int1, 0) = ^true) {
        autosetup_blackflaglast;
        if ($int4 = 0) {
            $int5 = detailget_toolkit_default;
            detail_toolkit($int5);
            ~script2593($int5, null);
        }
    } else {
        $int5 = detailget_toolkit;
    }
    if (testbit($int1, 1) = ^true & getwindowmode ! getdefaultwindowmode) {
        setwindowmode(getdefaultwindowmode);
    }
    if (testbit($int1, 2) = ^true) {
        detail_antialiasing(detailget_antialiasing_default);
    }
    if (testbit($int1, 3) = ^true) {
        detail_bloom(0);
    }
    if ($int2 = 3) {
        ~loginscreen_setactivemenu(0);
        ~autosetup($int2);
        return;
    }
    ~graphics_options_rebuild($int5, getwindowmode, ~graphics_options_reviewoptions($int5), $int2);
    if ($int2 = 1 & $int3 = 1) {
        mes("The requested change has been cancelled.");
    }
}
if ($int2 ! 1) {
    $int5 = detailget_toolkit;
    ~graphics_options_rebuild($int5, getwindowmode, ~graphics_options_reviewoptions($int5), $int2);
    if ($int2 = 2) {
        if_sethide(true, interface_906:com_52);
        if ($int4 = 0 & $int3 = 1) {
            ~lobbyscreen_input_full("", "The change of detail mode has been cancelled.<br><br>Perhaps different graphical settings would work better for you?", 0, -1, "", "", 0);
        }
    } else if ($int2 = 3) {
        if (%varclient_3684 ! 1 & ~userflowflags(3) = true) {
            ~loginscreen_setactivemenu(7);
        } else {
            %varclient_3684 = 0;
            ~loginscreen_setactivemenu(11);
        }
    } else if ($int2 = 4) {
        if_sethide(true, interface_1420:com_556);
        if_sethide(false, interface_1420:com_425);
    } else if ($int4 = 0 & $int3 = 1) {
        if_settext("The change of detail mode has been cancelled.<br><br>Perhaps different graphical settings would work better for you?", interface_744:com_89);
        if (testbit($int1, 1) = ^true) {
            if_setonclick("loginscreen_setactivemenu(6)", interface_744:com_92);
        } else {
            if_setonclick("loginscreen_setactivemenu_full(6, 0, 1)", interface_744:com_92);
        }
        ~loginscreen_setactivemenu(9);
    } else if (testbit($int1, 1) = ^true | $int4 = 1) {
        ~loginscreen_setactivemenu(6);
    } else {
        ~loginscreen_setactivemenu_full(6, 0, 1);
    }
}
if ($int0 = 0 & $int4 = 1) {
    ~autosetup($int2);
}
