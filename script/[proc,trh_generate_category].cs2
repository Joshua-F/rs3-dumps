// 16733
[proc,trh_generate_category](struct $struct0, int $int1)(int)
def_dbrow $dbrow2 = ~script9304($struct0);
if ($dbrow2 = null) {
    ~error("We are trying to bulid the TRH prizes but got a null $promo_data dbrow, falling back to default table.");
    $dbrow2 = dbrow_4562;
}
if (db_getfieldcount($dbrow2, dbtable_147:col0) = 0) {
    ~error("The non-null dbrow we are using to generate TRH prizes has no categories specified on it, falling back to the default table.");
    $dbrow2 = dbrow_4562;
}
def_int $int3 = db_getfieldcount($dbrow2, dbtable_147:col0);
def_int $int4 = 0;
def_int $intarray0(max($int3, db_getfieldcount(dbrow_4562, dbtable_147:col0)));
def_int $intarray1(max($int3, db_getfieldcount(dbrow_4562, dbtable_147:col0)));
def_int $int5 = 0;
def_dbrow $dbrow6 = null;
def_int $int7 = 0;
def_int $int8 = -1;
while (++$int8 < $int3) {
    $int4, $dbrow6, $int7 = db_getfield($dbrow2, dbtable_147:col0, $int8);
    if ($int1 = 1 & ~script16734($int4) = true | $int1 = 0) {
        $int5 = calc($int5 + ~script16165($int4, $dbrow2));
    }
    $intarray0($int8) = $int5;
    $intarray1($int8) = $int4;
}
if ($int5 = 0) {
    ~error("We found a dbrow defined for the current promo but it had no prize data, so are now using the default drop table.");
    $dbrow2 = dbrow_4562;
    $int4 = 0;
    $int8 = -1;
    $int3 = db_getfieldcount($dbrow2, dbtable_147:col0);
    while (++$int8 < $int3) {
        $int4, $dbrow6, $int7 = db_getfield($dbrow2, dbtable_147:col0, $int8);
        if ($int1 = 1 & ~script16734($int4) = true | $int1 = 0) {
            $int5 = calc($int5 + ~script16165($int4, $dbrow2));
        }
        $intarray0($int8) = $int5;
        $intarray1($int8) = $int4;
    }
}
def_int $int9 = random($int5);
$int8 = -1;
while (++$int8 < $int3) {
    if ($int9 < $intarray0($int8)) {
        return($intarray1($int8));
    }
}
~error("we were unable to roll a category into a player's slot, going to the specified fallback of: <tostring(db_getfield($dbrow2, dbtable_147:col4, 0))>");
return(db_getfield($dbrow2, dbtable_147:col4, 0));
