// 20263
[proc,script20263]
def_dbrow $dbrow0 = ~script20117(%league_active_player);
if (~script20132($dbrow0) = false) {
    ~script20254;
    return;
}
def_dbrow $dbrow1 = ~script20938($dbrow0);
def_dbrow $dbrow2 = db_getfield($dbrow1, league_relic_list:col0, %league_relic_selected_tier);
if ($dbrow2 = null) {
    ~script20254;
    return;
}
def_dbrow $dbrow3 = db_getfield($dbrow2, league_relic_tier:col1, %league_relic_selected_id);
if ($dbrow3 = null) {
    ~script20254;
    return;
}
~script20264($dbrow2, $dbrow3);
~script20268($dbrow1);
if_sethide(false, league_parent_relics:relic_info);
if_sethide(false, league_parent_relics:bg_passive);
if_sethide(true, league_parent_relics:relic_contents);
if_sethide(true, league_parent_relics:progress_bar);
if_sethide(true, league_parent_relics:relic_scrollbar);
if_sethide(true, league_parent_relics:passives_relic_title);
cc_deleteall(league_parent_relics:confirm_popup);
cc_deleteall(league_parent_relics:confirm_window);
def_string $string0 = db_getfield($dbrow3, league_relic:col0, 0);
def_graphic $graphic4 = db_getfield($dbrow3, league_relic:col7, 0);
def_boolean $boolean5 = ~league_relic_active($dbrow3);
def_int $int6 = db_getfield($dbrow3, league_relic:col2, 0);
def_int $int7 = db_getfield($dbrow2, league_relic_tier:col11, 0);
def_string $string1 = "Relics";
if ($int6 = 1) {
    $string1 = "Blessings";
}
if_settext("<~script21088($dbrow2, %league_relic_selected_tier)> <$string1>", league_parent_relics:bg_contents);
if ($int7 = 1) {
    if ($boolean5 = false & ~script20944($dbrow2) = true & ~script20947($dbrow2) = %league_relic_selected_id) {
        if_setenabled(true, league_parent_relics:body_confirm);
    } else {
        if_setenabled(false, league_parent_relics:body_confirm);
    }
} else if ($boolean5 = false & ~script20142(%league_relic_selected_tier) = true) {
    if_setenabled(true, league_parent_relics:body_confirm);
} else {
    if_setenabled(false, league_parent_relics:body_confirm);
}
def_int $int8 = 4;
def_int $int9 = 0;
def_int $int10 = if_getwidth(league_parent_relics:passive_info);
if (if_find(league_parent_relics:confirm_popup) = true) {
    $int9, $int8 = ~script20266($dbrow3, $dbrow2, $int9, $int8, $int10);
    $int9, $int8 = ~script20265($dbrow3, $int9, $int8, $int10, $boolean5);
    $int9, $int8 = ~script20267($dbrow3, $dbrow2, $int9, $int8, $int10);
}
~script19620(league_parent_relics:bg_confirm_popup, league_parent_relics:passive_info, $int8, null, -1, 0, 8, 4);
if_setsize(24, if_getheight(league_parent_relics:passive_info), ^setsize_minus, ^setsize_abs, league_parent_relics:passive_info);
