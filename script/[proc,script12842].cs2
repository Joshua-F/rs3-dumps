// 12842
[proc,script12842](int $int0)(int)
if (%varplayerbit_5994 ! 40) {
    return(0);
}
def_enum $enum1 = enum(int, enum, enum_5421, $int0);
if ($enum1 = null) {
    return(0);
}
def_int $int2 = enum_getoutputcount($enum1);
while (--$int2 >= 0) {
    if (compare("Dungeoneering", enum(int, string, $enum1, $int2)) = 0) {
        return($int2);
    }
}
return(0);
