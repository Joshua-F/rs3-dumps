// 17158
[proc,script17158](int $int0)(dbrow)
def_int $int1 = 5;
def_int $int2 = -1;
def_dbrow $dbrow3 = null;
while ($int1 <= 9) {
    $dbrow3 = ~trh184_get_bonus_dbrow($int1);
    if ($dbrow3 = null) {
        ~error("No dbrow for ID: <tostring($int1)>");
        return(null);
    }
    if ($int0 < db_getfield($dbrow3, dbtable_184:col5, 0)) {
        return(~trh184_get_bonus_dbrow($int2));
    } else {
        $int2 = $int1;
    }
    $int1 = calc($int1 + 1);
}
return(~trh184_get_bonus_dbrow($int2));
