// 90
[proc,filtertest](int $int0, int $int1)(boolean)
switch_int (chat_gethistorytype($int0)) {
    case null :
        return(false);
    case ^chattype_gamemessage, ^chattype_engine, ^chattype_obj_examine, ^chattype_npc_examine, ^chattype_loc_examine, ^chattype_modchat, 110, ^chattype_snapshotfeedback, ^chattype_friendnotification, ^chattype_ignorenotification, ^chattype_system_broadcast, ^chattype_npc_chat, ^chattype_clan_join :
        return(true);
    case ^chattype_spam :
        if (%varplayerbit_6161 = 1) {
            return(false);
        }
        return(true);
    case ^chattype_publicchat, ^chattype_quickchat_public :
        if (chat_getfilter_public = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_public = ^chatfilter_friends & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_privatechat, ^chattype_quickchat_private_incoming :
        if (%varplayer_287 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        if (chat_getfilter_private = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_private = ^chatfilter_friends & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_tradereq, ^chattype_challenge, 105, 106, ^chattype_ally_request, ^chattype_dungeon_invite, 112, ^chattype_conquest_regular_challenge, 114, ^chattype_rated_clan_war :
        if (chat_getfilter_trade = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_trade = ^chatfilter_friends & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_loginlogoutnotification, ^chattype_privatechatout, ^chattype_quickchat_private_outgoing :
        if (%varplayer_287 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        if (chat_getfilter_private < 2) {
            return(true);
        }
        return(false);
    case ^chattype_modprivatechat :
        if (%varplayer_287 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        return(true);
    case ^chattype_clanchat, ^chattype_clan_challenge, ^chattype_quickchat_clanchat, ^chattype_clanguestchat, ^chattype_quickchat_clanguestchat :
        if (%varplayer_1054 = 0) {
            return(true);
        }
        if (%varplayer_1054 = 1 & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_friendschat, ^chattype_quickchat_friend_chat :
        if (%varplayer_2159 = 0) {
            return(true);
        }
        if (%varplayer_2159 = 1 & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_assistreq :
        if (%varplayer_1055 = 0) {
            return(true);
        }
        if (%varplayer_1055 = 1 & friend_test(unknown_command_5019($int0)) = true) {
            return(true);
        }
        return(false);
    case ^chattype_friendschatnotification :
        if (%varplayer_2159 < 2) {
            return(true);
        }
        return(false);
    case ^chattype_clanmessage, ^chattype_clanguestmessage :
        if (%varplayer_1054 < 2) {
            return(true);
        }
        return(false);
    case ^chattype_trade_notification :
        if (chat_getfilter_trade < 2) {
            return(true);
        }
        return(false);
    case ^chattype_assist_notification :
        if (%varplayer_1055 < 2) {
            return(true);
        }
        return(false);
}
return(true);
