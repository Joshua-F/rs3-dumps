// 90
[proc,filtertest](int $int0, int $int1)(boolean)
def_int $chattype2 = ^chattype_gamemessage;
def_int $int3 = 0;
def_int $int4 = 0;
def_string $string0 = "";
def_string $string1 = "";
def_string $string2 = "";
def_string $string3 = "";
def_chatphrase $chatphrase5 = null;
def_string $string4 = "";
$chattype2, $int3, $int4, $string0, $string1, $string2, $string3, $chatphrase5, $string4 = chat_gethistory_byuid($int0);
switch_int ($chattype2) {
    case null :
        return(false);
    case ^chattype_gamemessage, ^chattype_engine, ^chattype_obj_examine, ^chattype_npc_examine, ^chattype_loc_examine, ^chattype_modchat, 110, ^chattype_snapshotfeedback, ^chattype_friendnotification, ^chattype_ignorenotification, ^chattype_system_broadcast, ^chattype_npc_chat, ^chattype_clan_join, ^chattype_all_panes :
        return(true);
    case ^chattype_spam :
        if (%varplayerbit_5999 = 1) {
            return(false);
        }
        return(true);
    case ^chattype_publicchat, ^chattype_quickchat_public :
        if (chat_getfilter_public = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_public = ^chatfilter_friends & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_privatechat, ^chattype_quickchat_private_incoming :
        if (%varplayer_457 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        if (chat_getfilter_private = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_private = ^chatfilter_friends & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_tradereq, ^chattype_challenge, 105, 106, ^chattype_ally_request, ^chattype_dungeon_invite, 112, ^chattype_conquest_regular_challenge, 114, ^chattype_rated_clan_war :
        if (chat_getfilter_trade = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_trade = ^chatfilter_friends & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_loginlogoutnotification, ^chattype_privatechatout, ^chattype_quickchat_private_outgoing :
        if (%varplayer_457 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        if (chat_getfilter_private < 2) {
            return(true);
        }
        return(false);
    case ^chattype_modprivatechat :
        if (%varplayer_457 > 0 & %varclient_41 >= 0 & $int1 = 0 & ~partnercheck($int0) = false) {
            return(false);
        }
        return(true);
    case ^chattype_clanchat, ^chattype_clan_challenge, ^chattype_quickchat_clanchat, ^chattype_clanguestchat, ^chattype_quickchat_clanguestchat :
        if (%varplayer_1772 = 0) {
            return(true);
        }
        if (%varplayer_1772 = 1 & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_friendschat, ^chattype_quickchat_friend_chat :
        if (%varplayer_1773 = 0) {
            return(true);
        }
        if (%varplayer_1773 = 1 & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_assistreq :
        if (%varplayer_1774 = 0) {
            return(true);
        }
        if (%varplayer_1774 = 1 & friend_test($string1) = true) {
            return(true);
        }
        return(false);
    case ^chattype_friendschatnotification :
        if (%varplayer_1773 < 2) {
            return(true);
        }
        return(false);
    case ^chattype_clanmessage, ^chattype_clanguestmessage :
        if (%varplayer_1772 < 2) {
            return(true);
        }
        return(false);
    case ^chattype_trade_notification :
        if (chat_getfilter_trade < 2) {
            return(true);
        }
        return(false);
    case ^chattype_ignorable_trade_notification :
        if (chat_getfilter_trade >= 2 | ignore_test($string0) = true) {
            return(false);
        }
    case ^chattype_assist_notification :
        if (%varplayer_1774 < 2) {
            return(true);
        }
        return(false);
    case 121 :
        if (%varplayerbit_16442 = 1) {
            return(false);
        } else if (%varplayerbit_5999 = 1) {
            return(false);
        }
    case ^chattype_raid_high :
        if (%varplayerbit_17864 = 1) {
            return(false);
        } else if (%varplayerbit_5999 = 1) {
            return(false);
        } else {
            return(true);
        }
    case ^chattype_raid_dnd :
        if (%varplayerbit_9784 = 1) {
            return(false);
        } else if (%varplayerbit_5999 = 1) {
            return(false);
        } else {
            return(true);
        }
    case 124 :
        if (%varplayerbit_18271 = 1 | %varplayerbit_3617 = 1) {
            return(false);
        } else if (%varplayerbit_5999 = 1) {
            return(false);
        } else {
            return(true);
        }
}
return(true);
