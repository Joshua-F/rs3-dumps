// 90
[proc,filtertest](int $int0)(boolean)
def_int $chattype1 = chat_gethistorytype($int0);
if ($chattype1 = null) {
    return(false);
}
if ($chattype1 = ^chattype_publicchat) {
    if (chat_getfilter_public = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_public = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype1 = ^chattype_privatechat) {
    if (%varplayer_287 = 1) {
        return(false);
    }
    if (chat_getfilter_private = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_private = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype1 = ^chattype_engine | $chattype1 = 8) {
    if (chat_getfilter_trade = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_trade = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype1 = ^chattype_loginlogoutnotification | $chattype1 = ^chattype_privatechatout) {
    if (%varplayer_287 = 1) {
        return(false);
    }
    if (chat_getfilter_private < 2) {
        return(true);
    }
    return(false);
}
if ($chattype1 = ^chattype_modprivatechat & %varplayer_287 = 1) {
    return(false);
}
return(true);
