// 90
[proc,script90](int $int0, int $int1)(boolean)
def_int $chattype2 = chat_gethistorytype($int0);
if ($chattype2 = null) {
    return(false);
}
if ($chattype2 = ^chattype_gamemessage | $chattype2 = ^chattype_modchat) {
    return(true);
}
if ($chattype2 = ^chattype_publicchat | $chattype2 = ^chattype_quickchat_public) {
    if (chat_getfilter_public = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_public = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype2 = ^chattype_privatechat | $chattype2 = ^chattype_quickchat_private_incoming) {
    if (%varplayer_287 = 1 & %varclient_41 >= 0 & $int1 = 0 & ~script506($int0) = false) {
        return(false);
    }
    if (chat_getfilter_private = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_private = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype2 = ^chattype_engine | $chattype2 = 8 | $chattype2 = 14 | $chattype2 = 15 | $chattype2 = 21) {
    if (chat_getfilter_trade = ^chatfilter_on) {
        return(true);
    }
    if (chat_getfilter_trade = ^chatfilter_friends & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype2 = ^chattype_loginlogoutnotification | $chattype2 = ^chattype_privatechatout | $chattype2 = ^chattype_quickchat_private_outgoing) {
    if (%varplayer_287 = 1 & %varclient_41 >= 0 & $int1 = 0 & ~script506($int0) = false) {
        return(false);
    }
    if (chat_getfilter_private < 2) {
        return(true);
    }
    return(false);
}
if ($chattype2 = ^chattype_modprivatechat) {
    if (%varplayer_287 = 1 & %varclient_41 >= 0 & $int1 = 0 & ~script506($int0) = false) {
        return(false);
    }
    return(true);
}
if ($chattype2 = ^chattype_friendschat | $chattype2 = 16 | $chattype2 = ^chattype_quickchat_friend_chat) {
    if (%varplayer_1054 = 0) {
        return(true);
    }
    if (%varplayer_1054 = 1 & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype2 = 10) {
    if (%varplayer_1055 = 0) {
        return(true);
    }
    if (%varplayer_1055 = 1 & friend_test(chat_gethistoryname($int0)) = true) {
        return(true);
    }
    return(false);
}
if ($chattype2 = ^chattype_friendschatnotification) {
    if (%varplayer_1054 < 2) {
        return(true);
    }
    return(false);
}
if ($chattype2 = 12) {
    if (chat_getfilter_trade < 2) {
        return(true);
    }
    return(false);
}
if ($chattype2 = 13) {
    if (%varplayer_1055 < 2) {
        return(true);
    }
    return(false);
}
return(true);
