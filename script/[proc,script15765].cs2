// 15765
[proc,script15765](struct $struct0, int $int1)(int, string)
def_int $int2 = 0;
switch_int ($int1) {
    case 1 :
        $int2 = %varplayerbit_24860;
    case 2 :
        $int2 = %varplayerbit_24863;
    case 3 :
        $int2 = %varplayerbit_24866;
    case 4 :
        $int2 = %varplayerbit_24869;
    case 5 :
        $int2 = %varplayerbit_24872;
}
if ($int2 = 1) {
    return(0, "You've already extended this challenge.");
}
if (struct_param($struct0, param_4551) = 0) {
    return(0, "This challenge cannot be extended.");
}
if (map_members = false) {
    return(0, "This feature can only be used on a members' world.");
}
def_struct $struct3 = null;
def_int $int4 = 1;
def_int $int5 = 0;
while ($int4 <= ~script15758) {
    $struct3 = ~script6783($int4);
    if ($struct3 ! null & ~script3841($int4) >= struct_param($struct3, param_2235) & ~script10583($int4) = 1) {
        if (~script10580($int4) = 1) {
            $int5 = calc($int5 + 2);
        } else {
            $int5 = calc($int5 + 1);
        }
    }
    $int4 = calc($int4 + 1);
}
if (calc(~script12869(struct_45613) + $int5 * 100) < 2100) {
    return(1, "Spend vis wax to double this challenge's XP, weekly progress and objective target.");
} else {
    return(1, "Spend vis wax to double this challenge's XP and objective target.");
}
