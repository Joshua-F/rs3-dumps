// 17872
[proc,script17872](dbrow $dbrow0)(int)
if ($dbrow0 = null) {
    ~error("Invalid Pass.");
    return(-1);
}
def_int $int1 = 0;
if ($dbrow0 = dbrow_7906) {
    $int1 = %varplayer_11347;
} else {
    if (db_getrowtable($dbrow0) ! dbtable_242) {
        ~error("Invalid Pass.");
        return(-1);
    }
    $int1 = var_reference_get(db_getfield($dbrow0, dbtable_242:col13, 0));
}
return($int1);
