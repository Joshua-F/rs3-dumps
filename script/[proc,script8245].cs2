// 8245
[proc,script8245]()(int)
def_int $int0 = 1;
def_enum $enum1 = enum_8429;
def_int $int2 = enum_getoutputcount($enum1);
def_dbrow $dbrow3 = null;
while ($int0 <= $int2) {
    $dbrow3 = enum(int, dbrow, $enum1, $int0);
    if ($dbrow3 ! null & (~script11205($dbrow3) = true | ~date_runeday >= date_runeday_fromdate(db_getfield($dbrow3, dbtable_77:col4, 0)) & ~date_runeday <= date_runeday_fromdate(db_getfield($dbrow3, dbtable_77:col5, 0)))) {
        return($int0);
    }
    $int0 = calc($int0 + 1);
}
return(-1);
