// 705
[proc,script705](int $int0, int $int1)(int)
def_int $int2 = ~script14353(0, calc(1320 - 1));
~error("Tab <tostring($int1)> returned slot <tostring($int0)> which is out of range (<tostring(1320)>). First freeslot is <tostring(%varplayer_8970)>. Empty proc found at: <tostring($int2)>. If the free slot isn't null that will be used instead.");
if ($int2 ! -1) {
    return($int2);
}
return($int0);
