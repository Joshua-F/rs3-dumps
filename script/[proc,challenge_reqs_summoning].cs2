// 6774
[proc,challenge_reqs_summoning](int $int0, int $int1)(int)
def_int $int2 = struct_param(enum(int, struct, enum_5517, $int0), param_2236);
if (stat_base(summoning) < $int2) {
    return(0);
}
if (map_members = false & $int1 = 0) {
    switch_int ($int0) {
        case 1, 2 :
            return(1);
        case default :
            return(0);
    }
    return(1);
}
switch_int ($int0) {
    case 6 :
        if (calc(date_runeday - %varplayerbit_5654) <= 6) {
            return(0);
        }
    case default :
        return(1);
}
return(1);
