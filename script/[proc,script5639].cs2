// 5639
[proc,script5639](int $int0, int $int1)(boolean)
def_string $string0 = "";
def_int $int2 = -1;
$string0 = %varclient_2559;
$int2 = 93061217;
%varclient_2561 = %varclient_2559;
def_int $int3 = string_length($string0);
if ($int3 <= 0) {
    ~create_error("Please enter your desired password here.", 93061217);
    return(false);
} else if ($int3 < 5) {
    ~create_error("Passwords must be at least 5 characters long.", $int2);
    return(false);
} else if ($int3 > 20) {
    ~create_error("Passwords must be no more than <tostring(20)> characters long.", $int2);
    return(false);
} else if (~script2202($string0) = true) {
    ~create_error("Passwords may only contain letters and numbers.", $int2);
    return(false);
} else if (string_indexof_string(%varclient_2555, $string0, 0) ! -1) {
    return(false);
} else if (string_indexof_string(%varclient_2563, $string0, 0) ! -1) {
    return(false);
}
def_string $string1 = substring($string0, 0, 1);
def_int $int4 = string_length($string0);
def_int $int5 = 0;
def_int $int6 = 0;
while ($int5 < $int4) {
    if (string_indexof_string($string0, $string1, $int5) = $int5) {
        $int6 = calc($int6 + 1);
    }
    $int5 = calc($int5 + 1);
}
if ($int6 = $int4) {
    ~create_error("Your password is too easy to guess.", $int2);
    return(false);
}
~script7845(1, "", 93061217);
if_sethide(true, interface_1420:com_316);
if ($int0 = 1) {
    ~create_focus(16, 1);
}
return(true);
