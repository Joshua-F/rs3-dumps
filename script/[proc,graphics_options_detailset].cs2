// 2582
[proc,graphics_options_detailset](struct $struct0, int $int1, int $int2)(int)
switch_struct ($struct0) {
    case struct_28783 :
        if ($int1 ! 2 & $int1 ! detailget_toolkit) {
            detail_toolkit($int1);
            if ($int1 ! detailget_toolkit) {
                return(0);
            }
            if ($int1 = 1 | $int1 = 3 | $int1 = 5) {
                ~script2700(1, $int2, 0);
                return(2);
            } else {
                detail_toolkit_default($int1, false);
            }
        }
    case struct_830 :
        detail_removeroofs_option($int1);
    case struct_831 :
        detail_grounddecor_on(~int_to_bool($int1));
    case struct_833 :
        detail_idleanims($int1);
    case struct_834 :
        detail_flickering_on(~int_to_bool($int1));
    case struct_836 :
        detail_spotshadows_on(~int_to_bool($int1));
    case struct_837 :
        detail_hardshadows($int1);
    case struct_838 :
        detail_lightdetail_high(~int_to_bool($int1));
    case struct_839, struct_33099 :
        if (detailcanset_waterdetail($int1) = 3) {
            return(0);
        }
        detail_waterdetail_high(~int_to_bool($int1));
    case struct_840 :
        detail_fog_on(~int_to_bool($int1));
    case struct_841 :
        detail_antialiasing($int1);
        if ($int1 > 0 & detailget_toolkit ! 0 & detailget_toolkit ! 2) {
            ~script2700(4, $int2, 0);
            return(2);
        }
        detail_antialiasing_default($int1);
    case struct_842 :
        if (detailcanmod_particles = false | detailcanset_particles($int1) = 0) {
            return(0);
        }
        detail_particles($int1);
    case struct_843 :
        detail_groundblending(~int_to_bool($int1));
    case struct_844 :
        %varclient_987 = ~int_to_bool($int1);
        detail_customcursors(%varclient_987);
    case struct_845 :
        %varclient_178 = $int1;
        if (getwindowmode = ^windowmode_fullscreen) {
            if (fullscreen_enter(fullscreen_getmode($int1)) = true) {
                if (has_nxt = false) {
                    ~script2700(2, $int2, 0);
                    return(2);
                } else {
                    return(1);
                }
            }
            return(0);
        }
    case struct_908 :
        detail_cpuusage($int1);
    case struct_963 :
        detail_texturing($int1);
    case struct_1009 :
        if (detailcanmod_maxscreensize = true & detailcanset_maxscreensize($int1) = 1) {
            if (%varclient_4108 ! 5 & %varclient_4108 ! -1) {
                ~script709(%varclient_4108, $int1);
                ~script8885(%varclient_4108);
            } else {
                detail_maxscreensize($int1);
            }
            autosetup_setcustom;
        } else {
            return(0);
        }
    case struct_2430 :
        if (detailcanmod_skydetail = false) {
            return(0);
        }
        if (detailcanset_skydetail($int1) = 3) {
            return(0);
        }
        detail_skydetail($int1);
    case struct_2858 :
        detail_bloom($int1);
    case struct_2841 :
        %varclient_1701 = $int1;
    case struct_33100 :
        if (detailcanset_bloom($int1) = 3) {
            return(0);
        }
        detail_bloom($int1);
    case struct_33101 :
        detail_drawdistance($int1);
    case struct_33102 :
        %varclient_3698 = $int1;
    case struct_33103 :
        if (detailcanset_antialiasingmode($int1) = 3) {
            return(0);
        }
        detail_antialiasingmode($int1);
    case struct_33104 :
        if (detailcanmod_antialiasing_quality = false | detailcanset_antialiasing_quality($int1) = 3) {
            return(0);
        }
        detail_antialiasing_quality($int1);
    case struct_33092 :
        if (detailcanset_hardshadows($int1) = 3) {
            return(0);
        }
        detail_hardshadows($int1);
    case struct_33093 :
        if (detailcanset_shadowquality($int1) = 3) {
            return(0);
        }
        detail_shadowquality($int1);
    case struct_33094 :
        if (detailcanset_lightingquality($int1) = 3) {
            return(0);
        }
        detail_lightingquality($int1);
    case struct_33097 :
        if (detailcanset_ambientocclusion($int1) = 3) {
            return(0);
        }
        detail_ambientocclusion($int1);
    case struct_33095 :
        if (detailcanset_texturing($int1) = 3) {
            return(0);
        }
        detail_texturing($int1);
    case struct_33096 :
        if (detailcanset_reflections($int1) = 3) {
            return(0);
        }
        detail_reflections($int1);
    case struct_33098 :
        if (detailcanset_vsync($int1) = 3) {
            return(0);
        }
        detail_vsync($int1);
    case struct_34879 :
        if (detailcanset_anisotropicfiltering($int1) = 3) {
            return(0);
        }
        detail_anisotropicfiltering($int1);
    case struct_34880 :
        if (detailcanset_volumetriclighting($int1) = 3) {
            return(0);
        }
        detail_volumetriclighting($int1);
    case struct_35785 :
        if (detailcanset_grounddecor($int1) = 3) {
            return(0);
        }
        detail_grounddecor_on(~int_to_bool($int1));
    case struct_35786 :
        if (detailcanmod_groundblending = false) {
            return(0);
        }
        detail_groundblending(~int_to_bool($int1));
    case struct_38730 :
        if (detailcanmod_maxforegroundfps = true & detailcanset_maxforegroundfps(calc((1 + $int1) * 5)) < 3) {
            detail_maxforegroundfps(calc((1 + $int1) * 5));
            return(1);
        }
        return(0);
    case struct_38731 :
        if (detailcanmod_maxbackgroundfps = true & detailcanset_maxbackgroundfps(calc((1 + $int1) * 5)) < 3) {
            detail_maxbackgroundfps(calc((1 + $int1) * 5));
            return(1);
        }
        return(0);
    case default :
        return(0);
}
~script2593(detailget_toolkit, $struct0);
return(1);
