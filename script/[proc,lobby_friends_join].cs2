// 1182
[proc,lobby_friends_join](int $int0)
if (worldlist_fetch = false) {
    if_setontimer("lobby_friends_join($int0)", interface_909:com_14);
    return;
} else {
    if_setontimer(null, interface_909:com_14);
}
def_int $int1 = 0;
def_string $string0 = "";
def_int $int2 = 0;
def_string $string1 = "";
$int2, $string0, $int1, $string0, $int1, $int1, $string1 = worldlist_specific($int0);
if ($int2 = -1) {
    mes("Sorry, you can't join that person.");
} else if (worldlist_switch($int0, $string1) = true) {
    %varclient_547 = 0;
    ~script3143(0, "Switched to game world <tostring($int0)>");
    ~lobbyscreen_entergame(interface_906:com_190);
} else {
    ~script3143(1, "Sorry, we couldn't contact world <tostring($int0)>.<br>Please choose a different world.");
    mes("Sorry, we couldn't contact world <tostring($int0)>. Please choose a different world.");
}
