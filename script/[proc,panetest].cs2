// 193
[proc,panetest](int $int0)(boolean)
def_int $chattype1 = ^chattype_gamemessage;
def_int $int2 = 0;
def_int $int3 = 0;
def_string $string0 = "";
def_string $string1 = "";
def_string $string2 = "";
def_string $string3 = "";
def_chatphrase $chatphrase4 = null;
def_string $string4 = "";
$chattype1, $int2, $int3, $string0, $string1, $string2, $string3, $chatphrase4, $string4 = chat_gethistory_byuid($int0);
if (%varclient_41 = 0) {
    return(true);
}
if ($chattype1 = null) {
    return(false);
} else if ($chattype1 = ^chattype_system_broadcast | $chattype1 = ^chattype_all_panes) {
    return(true);
}
switch_int (%varclient_41) {
    case 1 :
        if ($chattype1 = ^chattype_gamemessage | $chattype1 = ^chattype_engine | $chattype1 = ^chattype_obj_examine | $chattype1 = ^chattype_npc_examine | $chattype1 = ^chattype_loc_examine | $chattype1 = ^chattype_spam | $chattype1 = 110 | $chattype1 = ^chattype_snapshotfeedback | $chattype1 = ^chattype_clan_join) {
            return(true);
        }
    case 2 :
        if ($chattype1 = ^chattype_modchat | $chattype1 = ^chattype_publicchat | $chattype1 = ^chattype_quickchat_public | $chattype1 = ^chattype_npc_chat) {
            return(true);
        }
    case 3 :
        switch_int ($chattype1) {
            case ^chattype_privatechat, ^chattype_privatechatout, ^chattype_loginlogoutnotification, ^chattype_quickchat_private_incoming, ^chattype_quickchat_private_outgoing, ^chattype_modprivatechat, ^chattype_friendnotification, ^chattype_ignorenotification :
                return(true);
        }
    case 4 :
        switch_int ($chattype1) {
            case ^chattype_friendschat, ^chattype_friendschatnotification, ^chattype_quickchat_friend_chat :
                return(true);
        }
    case 5 :
        switch_int ($chattype1) {
            case ^chattype_tradereq, ^chattype_challenge, ^chattype_trade_notification, 105, 106, ^chattype_ally_request, ^chattype_dungeon_invite, 112, ^chattype_conquest_regular_challenge, 114, ^chattype_rated_clan_war, ^chattype_ignorable_trade_notification :
                return(true);
        }
        if (~partnercheck($int0) = true) {
            return(true);
        }
    case 6 :
        if ($chattype1 = ^chattype_assistreq | $chattype1 = ^chattype_assist_notification) {
            return(true);
        }
    case 7 :
        switch_int ($chattype1) {
            case ^chattype_clanchat, ^chattype_clanmessage, ^chattype_clan_challenge, ^chattype_quickchat_clanchat, ^chattype_clanguestchat, ^chattype_clanguestmessage, ^chattype_quickchat_clanguestchat :
                return(true);
        }
}
return(false);
