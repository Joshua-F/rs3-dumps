// 9642
[clientscript,script9642](int $int0, component $component1, coord $coord2)
if (clientclock >= $int0) {
    if (has_html5 = true) {
        ~script7955($coord2);
    } else {
        worldmap_jumptodisplaycoord($coord2);
    }
    if_setontimer(null, $component1);
}
