// 11607
[clientscript,slayer_window_tooltip](component $component0, int $int1)
def_int $int2 = 0;
def_int $int3 = 0;
def_string $string0 = "";
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = -1;
def_string $string1 = "";
def_string $string2 = "";
$int2, $int3, $string0, $int4, $int5 = ~script11608;
if ($int2 = 0) {
    if ($int1 = 1) {
        $string1 = ~script14060;
    } else {
        $string1 = ~script16090;
    }
    if (compare($string1, "No Task") = 0) {
        if ($int1 = 0) {
            ~script8800("You need to speak to a slayer master for a new assignment.", $component0, -1);
        } else {
            ~script8800("You need to speak to Death for a new assignment.", $component0, -1);
        }
    } else if ($int1 = 0) {
        $string2 = ~script16093;
        if (string_length($string2) > 0) {
            ~script8800("<tostring(%varplayer_183)> <$string1> left. <$string2>", $component0, -1);
        } else {
            ~script8800("<tostring(%varplayer_183)> <$string1> left.", $component0, -1);
        }
    } else {
        ~script8800("<tostring(%varplayerbit_22902)> <$string1> left.", $component0, -1);
    }
} else {
    ~script8800($string0, $component0, -1);
}
