// 3560
[proc,script3560](component $component0)
def_int $int1 = 0;
def_component $component2 = null;
def_struct $struct3 = null;
if (if_find($component0) = true & cc_param(param_8102) = true) {
    mes_typed(^chattype_console, 0, "Inactive list.");
    $component2 = cc_param(param_8101);
    while (.if_find($component2) = true) {
        if (++$int1 > 50) {
            return;
        }
        if (.cc_param(param_8102) = false) {
            $struct3 = .cc_param(param_8106);
            if ($struct3 ! null) {
                mes_typed(^chattype_console, 0, "<tostring($int1)> - <struct_param($struct3, param_2794)>");
            } else {
                mes_typed(^chattype_console, 0, "<tostring($int1)> - null");
            }
        }
        $component2 = .cc_param(param_8105);
    }
    $int1 = 0;
    mes_typed(^chattype_console, 0, "Active list.");
    $component2 = cc_param(param_8100);
    while (.if_find($component2) = true) {
        if (++$int1 > 50) {
            return;
        }
        if (.cc_param(param_8102) = false) {
            $struct3 = .cc_param(param_8106);
            if ($struct3 ! null) {
                mes_typed(^chattype_console, 0, "<tostring($int1)> - <struct_param($struct3, param_2794)>");
            } else {
                mes_typed(^chattype_console, 0, "<tostring($int1)> - null");
            }
        }
        $component2 = .cc_param(param_8105);
    }
}
return;
