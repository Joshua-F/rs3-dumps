// 15127
[proc,event_crafting_recipe_get_active_ingredient_info](dbrow $dbrow0, int $int1)(obj, int, int, boolean)
if ($dbrow0 = null) {
    ~error("event_crafting_recipe_get_ingredient_info called without a recipe!");
    return(null, 0, 0, false);
}
def_dbrow $dbrow2 = null;
if (%varplayerbit_47735 > 0) {
    db_find(dbtable_106:col0, %varplayerbit_47735);
    $dbrow2 = db_findnext;
}
if (db_getfieldcount($dbrow0, dbtable_106:col4) <= $int1) {
    ~error("event_crafting_recipe_get_ingredient_info looking for slot <tostring($int1)> however it is OUT OF BOUNDS on this recipe.");
    return(null, 0, 0, false);
}
def_obj $obj3 = null;
def_int $int4 = 0;
$obj3, $int4 = db_getfield($dbrow0, dbtable_106:col4, $int1);
if ($obj3 = null) {
    ~error("We're expecting something at slot <tostring($int1)> in event_crafting_recipe_get_active_ingredient_info.");
}
if ($dbrow0 ! $dbrow2) {
    return($obj3, 0, $int4, false);
}
def_obj $obj5 = inv_getobj(inv_894, $int1);
def_int $int6 = inv_getnum(inv_894, $int1);
if ($obj5 ! null & $obj5 ! $obj3) {
    ~error("Event crafting inventory is OUT of sync! We're expecting a <oc_name($obj3)> and we got a <oc_name($obj5)> while crafting recipe ID <tostring(%varplayerbit_47735)>");
}
def_boolean $boolean7 = ~script15128($int1);
if ($int6 = $int4 & $boolean7 = false) {
    ~error("Active recipe <tostring(%varplayerbit_47735)>, should've had its 'completed var set, but didn't. (Has it not transmitted yet?)");
}
return($obj3, $int6, $int4, $boolean7);
