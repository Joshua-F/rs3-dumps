// 5393
[proc,script5393]()(int)
def_int $int0 = -1;
def_int $int1 = -1;
if (activeclanchannel_find_affined = true) {
    if (compare(%varclient_2353, removetags(chat_playername_unfiltered)) = 0) {
        return(1);
    }
    $int1 = activeclanchannel_getuserslot(removetags(chat_playername_unfiltered));
    if ($int1 >= 0) {
        $int0 = activeclanchannel_getuserrank($int1);
        if (~script5963($int0) = 1) {
            return(1);
        }
    }
}
return(0);
