// 5393
[proc,script5393]()(int)
def_int $int0 = -1;
def_int $int1 = -1;
if (activeclanchannel_find_affined = true) {
    $int1 = activeclanchannel_getuserslot(removetags(chat_playername));
    if ($int1 >= 0) {
        $int0 = activeclanchannel_getuserrank($int1);
        if ($int0 >= 100) {
            return(1);
        }
    }
}
return(0);
