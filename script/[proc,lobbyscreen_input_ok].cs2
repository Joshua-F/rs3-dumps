// 3020
[proc,lobbyscreen_input_ok](int $int0, string $string0)
def_int $int1 = 0;
def_string $string1 = "";
def_string $string2 = "";
def_int $int2 = -1;
def_string $string3 = "";
def_string $string4 = "";
def_int $int3 = -1;
if (string_length(%varclientstring_277) > 0) {
    switch_int ($int0) {
        case 0 :
            if (string_length(%varclientstring_277) > 0 & string_length($string0) > 0) {
                if (chat_getfilter_private = ^chatfilter_off) {
                    ~script3047(1);
                }
                chat_sendprivate(removetags($string0), %varclientstring_277);
            }
        case 1 :
            if (friend_count < 0) {
                mes("Unable to update Friends List - system busy.");
            } else if (string_length(%varclientstring_277) > 0) {
                friend_add(%varclientstring_277);
            }
        case 2 :
            if (ignore_count < 0) {
                mes("Unable to update Ignore List - system busy.");
            } else if (string_length(%varclientstring_277) > 0) {
                ignore_add(%varclientstring_277);
            }
        case 3 :
            if (friend_count < 0) {
                mes("Unable to update Friends List - system busy.");
            } else if (string_length(%varclientstring_277) > 0) {
                friend_del(%varclientstring_277);
            }
        case 4 :
            if (ignore_count < 0) {
                mes("Unable to update Ignore List - system busy.");
            } else if (string_length(%varclientstring_277) > 0) {
                ignore_del(%varclientstring_277);
            }
        case 5 :
            if (clan_getchatcount <= 0 & string_length(%varclientstring_277) > 0) {
                clan_joinchat(%varclientstring_277);
            }
        case 6 :
            %varclient_1094 = 0;
            openurl("bugtracker_v4", "index.html", false);
        case 7 :
            if (string_length(%varclientstring_277) > 0) {
                ~clan_chat_kick_find(%varclientstring_277);
            }
        case 9 :
            if (clan_getchatcount > 0 & string_length(%varclientstring_277) > 0) {
                ~friendschat_kick(%varclientstring_277);
            }
        case 10 :
            unknown_command_5630;
            %varclient_1100 = -1;
        case 12 :
            ~lobbyscreen_input_close;
            unknown_command_5630;
            %varclient_1100 = -1;
            ~script4701(1, %varclient_1553, %varclient_1554, %varclientstring_128);
            return;
    }
}
%varclient_1650 = 0;
if ($int1 = 1) {
    ~lobbyscreen_input($string1, $string2, $int2, $string3, $string4);
} else {
    ~lobbyscreen_input_close;
}
