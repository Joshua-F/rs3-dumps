// 7973
[proc,script7973](component $component0, int $int1)
def_string $string0 = "The action bar is locked.<br>Click here to unlock it.";
def_string $string1 = "Unlock action bar";
if (%varplayerbit_1892 = 1) {
    if ($int1 = 1) {
        ~script6739(12);
    }
    if (if_getgraphic($component0) = hud_combat_btn_locked_over) {
    } else if (if_getgraphic($component0) = hud_combat_btn_unlocked_over) {
        if_setgraphic(hud_combat_btn_locked_over, $component0);
    } else if (if_getgraphic($component0) = hud_combat_btn_unlocked) {
        if_setgraphic(hud_combat_btn_locked, $component0);
    } else {
    }
} else {
    $string0 = "The action bar is unlocked.<br>Click here to lock it.";
    $string1 = "Lock action bar";
    if ($int1 = 1) {
        ~script6739(13);
    }
    if (if_getgraphic($component0) = hud_combat_btn_locked_over) {
        if_setgraphic(hud_combat_btn_unlocked_over, $component0);
    } else if (if_getgraphic($component0) = hud_combat_btn_unlocked_over) {
    } else if (if_getgraphic($component0) = hud_combat_btn_unlocked) {
    } else {
        if_setgraphic(hud_combat_btn_unlocked, $component0);
    }
}
if_setonmouserepeat("script8799($string0, event_com, event_comsubid)", interface_1430:com_21);
if_setonmouseleave("script8805", interface_1430:com_21);
~script8808;
if_setop(1, $string1, interface_1430:com_21);
~script6993(0, 0, 0, -1);
