// 7106
[proc,script7106](int $int0, int $int1, int $int2, boolean $boolean3, int $int4)(boolean)
if ($int0 > 0 & $int0 < 61) {
    if ($boolean3 = true) {
        if (stat(enum(int, stat, enum_681, $int0)) >= $int1 | ~assist_stat_int($int0) = true & $int2 = 0 & ~makex2012_get_assist_level($int4) >= $int1) {
            return(true);
        }
    } else if ($boolean3 = false & (stat_base(enum(int, stat, enum_681, $int0)) >= $int1 | ~assist_stat_int($int0) = true & $int2 = 0 & ~makex2012_get_assist_level($int4) >= $int1)) {
        return(true);
    }
    return(false);
}
switch_int ($int0) {
    case 61 :
        if (~ql4_getprogress($int1) = 2) {
            return(true);
        } else {
            return(false);
        }
    case 62 :
        return(~script7163($int1));
    case 63 :
        return(~int_to_bool(~invent_blueprint_unlocked($int1)));
    case 64 :
        if (%varplayer_9409 = $int1) {
            return(true);
        }
        return(false);
    case 65 :
        return(~int_to_bool(~arch_research_get_complete_state(~arch_research_get_data($int1))));
    case 66 :
        return(~int_to_bool(~invent_blueprint_unlocked($int1)));
    case default :
        return(true);
}
