// 16282
[proc,script16282](int $int0)
if (~script11772 = false) {
    return;
}
~mtx_benefits_draw_title($int0, "Cosmetics to claim");
db_listall(dbtable_181);
def_dbrow $dbrow1 = db_findnext;
while ($dbrow1 ! null) {
    if (~script11172($dbrow1) = true) {
        ~script16269($int0, "As you've purchased Premier Membership, you're eligible to claim '<struct_param(db_getfield($dbrow1, dbtable_181:col2, 0), param_2533)>'. Press the button to unlock them.", db_getfield($dbrow1, dbtable_181:col3, 0), db_getfield($dbrow1, dbtable_181:col4, 0));
    }
    $dbrow1 = db_findnext;
}
