// 20083
[proc,script20083](int $int0, int $int1)(int)
def_int $int2 = 0;
if (%varclient_4272 > 0) {
    $int2 = calc($int1 * 3);
} else if (%varclient_999 > 0) {
    $int2 = calc($int1 * 2);
} else if (%varclient_998 > 0) {
    $int2 = $int1;
}
switch_int ($int0) {
    case 97 :
        return($int2);
    case 139 :
        if (~script20078(97) = false) {
            $int2 = calc($int2 + $int1);
        }
        return($int2);
    case 249 :
        if (~script20078(97) = false) {
            $int2 = calc($int2 + $int1);
        }
        if (~script20078(139) = false) {
            $int2 = calc($int2 + $int1);
        }
        return($int2);
    case 252 :
        if (~script20078(97) = false) {
            $int2 = calc($int2 + $int1);
        }
        if (~script20078(139) = false) {
            $int2 = calc($int2 + $int1);
        }
        if (~script20078(249) = false) {
            $int2 = calc($int2 + $int1);
        }
        return($int2);
}
