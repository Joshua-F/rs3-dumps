// 192
[proc,chatalerts]
if (%varplayer_1295 < 1000) {
    return;
}
def_int $chattype0 = ^chattype_gamemessage;
def_int $int1 = 0;
def_int $int2 = 0;
def_string $string0 = "";
def_string $string1 = "";
def_string $string2 = "";
def_string $string3 = "";
def_chatphrase $chatphrase3 = null;
def_string $string4 = "";
def_int $int4 = %varclient_43;
def_int $int5 = ~script1960;
if ($int4 < $int5) {
    if (%varclient_41 = 0) {
        return;
    }
    $int4 = calc($int4 + 1);
    while ($int4 <= $int5) {
        $chattype0, $int1, $int2, $string0, $string1, $string2, $string3, $chatphrase3, $string4 = chat_gethistory_byuid($int4);
        if (~filtertest($int4, 0) = true & ~partnercheck($int4) = false) {
            switch_int ($chattype0) {
                case ^chattype_gamemessage, ^chattype_engine, ^chattype_obj_examine, ^chattype_npc_examine, ^chattype_loc_examine, ^chattype_spam, 110, ^chattype_snapshotfeedback, ^chattype_clan_join, 121, ^chattype_raid_high, ^chattype_raid_dnd, 124 :
                    ~chat_alert_enable(1);
                case ^chattype_modchat, ^chattype_publicchat, ^chattype_quickchat_public, ^chattype_npc_chat :
                    ~chat_alert_enable(2);
                case ^chattype_privatechat, ^chattype_loginlogoutnotification, ^chattype_privatechatout, ^chattype_modprivatechat, ^chattype_quickchat_private_incoming, ^chattype_quickchat_private_outgoing, ^chattype_friendnotification, ^chattype_ignorenotification :
                    ~chat_alert_enable(3);
                case ^chattype_friendschat, ^chattype_friendschatnotification, ^chattype_clan_challenge, ^chattype_quickchat_friend_chat :
                    ~chat_alert_enable(4);
                case ^chattype_tradereq, ^chattype_challenge, ^chattype_trade_notification, 105, 106, ^chattype_ally_request, ^chattype_dungeon_invite, 112, ^chattype_conquest_regular_challenge, 114, ^chattype_rated_clan_war, ^chattype_ignorable_trade_notification :
                    ~chat_alert_enable(5);
                case ^chattype_assistreq, ^chattype_assist_notification :
                    ~chat_alert_enable(6);
                case ^chattype_clanchat, ^chattype_clanmessage, ^chattype_quickchat_clanchat :
                    ~chat_alert_enable(7);
                case ^chattype_clanguestchat, ^chattype_clanguestmessage, ^chattype_quickchat_clanguestchat :
                    ~chat_alert_enable(7);
                case ^chattype_system_broadcast :
                    if (string_length($string4) > 1) {
                        ~chat_alert_enable(1);
                    }
                case ^chattype_all_panes :
                    if (%varclient_41 = -1) {
                        ~chat_alert_enable(1);
                    }
            }
        }
        $int4 = calc($int4 + 1);
    }
}
