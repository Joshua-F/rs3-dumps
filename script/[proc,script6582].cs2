// 6582
[proc,script6582](int $int0)(int)
switch_int ($int0) {
    case 556 :
        if (inv_totalparam(inv_94, param_972) > 0) {
            return(1);
        }
    case 555 :
        if (inv_totalparam(inv_94, param_973) > 0) {
            return(1);
        }
    case 557 :
        if (inv_totalparam(inv_94, param_974) > 0) {
            return(1);
        }
    case 554 :
        if (inv_totalparam(inv_94, param_975) > 0) {
            return(1);
        }
    case 559 :
        if (inv_totalparam(inv_94, param_2513) > 0) {
            return(1);
        }
    case 558 :
        if (inv_totalparam(inv_94, param_2512) > 0) {
            return(1);
        }
    case 564 :
        if (inv_totalparam(inv_94, param_2518) > 0) {
            return(1);
        }
    case 562 :
        if (inv_totalparam(inv_94, param_2514) > 0) {
            return(1);
        }
    case 561 :
        if (inv_totalparam(inv_94, param_2519) > 0) {
            return(1);
        }
    case 560 :
        if (inv_totalparam(inv_94, param_2515) > 0) {
            return(1);
        }
    case 563 :
        if (inv_totalparam(inv_94, param_2521) > 0) {
            return(1);
        }
    case 566 :
        if (inv_totalparam(inv_94, param_2517) > 0) {
            return(1);
        }
    case 565 :
        if (inv_totalparam(inv_94, param_2516) > 0) {
            return(1);
        }
    case 9075 :
        if (inv_totalparam(inv_94, param_2520) > 0) {
            return(1);
        }
    case 58450 :
        if (inv_totalparam(inv_94, param_9236) > 0) {
            return(1);
        }
}
return(0);
