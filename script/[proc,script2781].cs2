// 2781
[proc,script2781]()(string)
def_string $string0 = "";
def_int $int0 = date_runeday;
def_int $int1 = userdetail_lobby_membership;
def_int $int2 = userdetail_lobby_ccexpiry;
def_int $int3 = userdetail_lobby_graceexpiry;
def_int $int4 = calc($int3 - $int0);
if ($int3 <= $int2 | $int4 < 0) {
    $int4 = 0;
}
if ($int2 ! 0 & calc($int1 + $int0) >= $int2 & %varclient_1315 ! $int0 & $int1 <= 14) {
    if ($int1 <= 7) {
        if ($int1 <= 3) {
            if ($int1 <= 1) {
                if ($int1 = 0) {
                    if ($int4 > 0 & $int4 <= 14) {
                        if ($int4 <= 7) {
                            if ($int4 <= 3) {
                                if ($int4 = 1) {
                                    $string0 = "Your credit card has expired and your membership credit has run out. Please re-subscribe using a new credit card to restart your membership.<br><br>If you renew today, you will pay the lower 'loyalty' rate. After this, your discount will no longer be available.";
                                    %varclient_1316 = $int0;
                                } else if (calc(%varclient_1316 + 2) < $int0) {
                                    $string0 = "Your credit card has expired and your membership credit has run out. Please re-subscribe using a new credit card to restart your membership.<br><br>If you renew within the next <tostring($int4)> days, you will pay the lower 'loyalty' rate. After this, your discount will no longer be available.";
                                    %varclient_1316 = $int0;
                                }
                            } else if (calc(%varclient_1316 + 4) < $int0) {
                                $string0 = "Your credit card has expired and your membership credit has run out. Please re-subscribe using a new credit card to restart your membership.<br><br>If you renew within the next <tostring($int4)> days, you will pay the lower 'loyalty' rate. After this, your discount will no longer be available.";
                                if ($int4 = 7) {
                                    %varclient_1316 = $int0;
                                } else {
                                    %varclient_1316 = calc($int0 - (7 - $int4));
                                }
                            }
                        } else if (calc(%varclient_1316 + 7) < $int0) {
                            $string0 = "Your credit card has expired and your membership credit has run out. Please re-subscribe using a new credit card to restart your membership.<br><br>If you renew within the next <tostring($int4)> days, you will pay the lower 'loyalty' rate. After this, your discount will no longer be available.";
                            if ($int4 = 14) {
                                %varclient_1316 = $int0;
                            } else {
                                %varclient_1316 = calc($int0 - (14 - $int4));
                            }
                        }
                    }
                } else {
                    if ($int4 > 0) {
                        $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.<br><br>If you renew within the next <tostring($int4)> days, you will continue paying your lower 'loyalty' rate. After this, your discount will no longer be available.";
                    } else {
                        $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.";
                    }
                    %varclient_1315 = $int0;
                }
            } else if (calc(%varclient_1315 + 2) < $int0) {
                if ($int4 > 0) {
                    $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.<br><br>If you renew within the next <tostring($int4)> days, you will continue paying your lower 'loyalty' rate. After this, your discount will no longer be available.";
                } else {
                    $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.";
                }
                if ($int1 = 3) {
                    %varclient_1315 = $int0;
                } else {
                    %varclient_1315 = calc($int0 - (3 - $int1));
                }
            }
        } else if (calc(%varclient_1315 + 4) < $int0) {
            if ($int4 > 0) {
                $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.<br><br>If you renew within the next <tostring($int4)> days, you will continue paying your lower 'loyalty' rate. After this, your discount will no longer be available.";
            } else {
                $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.";
            }
            if ($int1 = 7) {
                %varclient_1315 = $int0;
            } else {
                %varclient_1315 = calc($int0 - (7 - $int1));
            }
        }
    } else if (calc(%varclient_1315 + 7) < $int0) {
        if ($int4 > 0) {
            $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.<br><br>If you renew within the next <tostring($int4)> days, you will continue paying your lower 'loyalty' rate. After this, your discount will no longer be available.";
        } else {
            $string0 = "Your credit card has expired. Please re-subscribe using a new credit card to continue your membership.";
        }
        if ($int1 = 14) {
            %varclient_1315 = $int0;
        } else {
            %varclient_1315 = calc($int0 - (14 - $int1));
        }
    }
}
return($string0);
