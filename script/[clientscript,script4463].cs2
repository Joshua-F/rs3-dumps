// 4463
[clientscript,script4463](int $int0, int $int1)
def_string $string0 = "";
def_int $int2 = 0;
if (activeclanchannel_find_affined = true) {
    if (activeclansettings_find_affined = true) {
        $string0 = activeclanchannel_getuserdisplayname($int0);
        if ($int1 = 1) {
            if (friend_test($string0) = true) {
                $int2 = 1;
            } else {
                mes_typed(^chattype_gamemessage, 0, "You only message people on your Friends List.");
            }
        } else if ($int1 = 8) {
            ~clan_chat_kick($string0);
        }
    } else {
        mes("You must be in a clan to do that.");
    }
} else {
    mes("You must be in a clan to do that.");
}
if ($int2 = 1) {
    %varclient_1650 = 1;
    %varclientstring_23 = $string0;
    ~script1558(false);
    return;
}
