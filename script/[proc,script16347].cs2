// 16347
[proc,script16347](struct $struct0)(string)
def_dbrow $dbrow1 = struct_param($struct0, param_4921);
if ($dbrow1 = null) {
    ~error("Attempted to get desc of promo <tostring(enum_getreverseindex(struct, int, enum_12135, $struct0, 0))> which did not have a dbrow specified on trh_promo_info_dbrow");
    return("");
}
if (db_getrowtable($dbrow1) ! dbtable_156) {
    ~error("Attempted to get desc of promo <tostring(enum_getreverseindex(struct, int, enum_12135, $struct0, 0))> but the dbrow on param trh_promo_info_dbrow was invalid");
    return("");
}
return(~script16348($dbrow1));
