// 8735
[clientscript,script8735](int $int0, component $component1, component $component2)
~script7772($int0, $component1, $component2);
def_int $int3 = enum(int, int, enum_7710, $int0);
if (~script8699($int3) = 0) {
    mes("That layout doesn't have any data saved to it yet. Pick another one.");
    return;
} else if (has_html5 = true & $int3 = 4) {
    mes("Old School preset is only available in the Java version.");
    return;
} else {
    ~script8741($int3);
}
