// 19933
[proc,script19933](dbrow $dbrow0)(int)
def_int $int1 = 0;
def_dbrow $dbrow2 = null;
unknown_command_77(db_filter_value(dbtable_319:col1, $dbrow0, ^filterop_eq, 0), dbtable_319:col0, true);
$dbrow2 = db_findnext;
while ($dbrow2 ! null) {
    if (var_reference_get(db_getfield($dbrow2, dbtable_319:col4, 0)) = 1) {
        $int1 = calc($int1 + 1);
    }
    $dbrow2 = db_findnext;
}
return($int1);
