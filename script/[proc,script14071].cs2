// 14071
[proc,script14071](enum $enum0, int $int1, int $int2)(boolean)
def_int $int3 = 5;
switch_enum ($enum0) {
    case enum_13023, enum_13024, enum_13025, enum_13026, enum_13027, enum_13028 :
        if ($int1 < calc($int2 / 2)) {
            return(true);
        }
}
switch_enum ($enum0) {
    case enum_13022 :
        if (stat_base_actual(woodcutting) >= $int3 & stat_base_actual(firemaking) >= $int3 & stat_base_actual(fishing) >= $int3 & stat_base_actual(cooking) >= $int3) {
            return(true);
        }
    case enum_10909 :
        if (stat_base_actual(attack) >= 90 & stat_base_actual(ranged) >= 90 & stat_base_actual(magic) >= 90) {
            return(true);
        }
    case enum_10884 :
        if (%varplayer_1297 >= 200) {
            return(true);
        }
    case enum_13023 :
        if (stat_base_actual(fletching) >= $int3 & stat_base_actual(crafting) >= $int3 & stat_base_actual(ranged) >= $int3) {
            return(true);
        }
    case enum_13024 :
        if (stat_base_actual(mining) >= $int3 & stat_base_actual(smithing) >= $int3 & stat_base_actual(attack) >= $int3 & stat_base_actual(strength) >= $int3) {
            return(true);
        }
    case enum_13025 :
        if (stat_base_actual(farming) >= $int3 & stat_base_actual(cooking) >= $int3 & stat_base_actual(thieving) >= $int3) {
            return(true);
        }
    case enum_13026 :
        if (stat_base_actual(hunter) >= $int3 & stat_base_actual(prayer) >= $int3 & stat_base_actual(herblore) >= $int3 & stat_base_actual(summoning) >= $int3) {
            return(true);
        }
    case enum_13027 :
        if (stat_base_actual(agility) >= $int3 & stat_base_actual(runecraft) >= $int3 & stat_base_actual(magic) >= $int3) {
            return(true);
        }
    case enum_13028 :
        if (stat_base_actual(slayer) >= $int3 & stat_base_actual(dungeoneering) >= $int3 & stat_base_actual(construction) >= $int3) {
            return(true);
        }
    case enum_13029 :
        if (%varplayerbit_15513 = 1) {
            return(true);
        }
    case enum_13030 :
        if (%varplayerbit_15630 = 1) {
            return(true);
        }
}
return(false);
