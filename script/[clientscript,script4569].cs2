// 4569
[clientscript,script4569](int $int0, string $string0, string $string1, int $int1)
switch_int ($int0) {
    case 1 :
        if (friend_test(removetags($string1)) = false) {
            friend_add(removetags($string1));
        }
    case 2 :
        if (friend_test(removetags($string1)) = false) {
            ignore_add(removetags($string1));
        }
    case 3 :
        ~lobbyscreen_input("Send message to <$string0>", "", 0, $string1, "");
    case 5 :
        %varclient_2481 = removetags($string0);
        ~script3190;
    case 6 :
        if (string_length(enum(int, string, enum_15193, $int1)) > 0) {
            switch_int (map_lang) {
                case default :
                    openurlraw(enum(int, string, enum_15193, $int1), true);
                case 1 :
                    openurlraw(enum(int, string, enum_15206, $int1), true);
                case 2 :
                    openurlraw(enum(int, string, enum_15207, $int1), true);
                case 3 :
                    openurlraw(enum(int, string, enum_15208, $int1), true);
            }
        }
    case 10 :
        ~friendschat_kick(removetags($string1));
}
