// 14492
[proc,script14492](obj $obj0, int $int1)(boolean)
switch_int ($int1) {
    case 223 :
        if (%quest_dm_main = 60) {
            return(true);
        } else {
            return(false);
        }
    case 332 :
        if (%player_fort_building_workshop >= 3) {
            return(true);
        }
        if (%player_fort_building_townhall >= 3) {
            return(true);
        }
        if (%player_fort_building_command_centre >= 3) {
            return(true);
        }
        if (%player_fort_building_chapel >= 3) {
            return(true);
        }
        if (%player_fort_building_kitchen >= 3) {
            return(true);
        }
        if (%player_fort_building_guardhouse >= 3) {
            return(true);
        }
        if (%player_fort_building_grove >= 3) {
            return(true);
        }
        if (%player_fort_building_rangers_workroom >= 3) {
            return(true);
        }
        if (%player_fort_building_botanists_workbench >= 3) {
            return(true);
        }
        return(false);
}
return(true);
