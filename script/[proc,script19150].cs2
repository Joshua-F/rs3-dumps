// 19150
[proc,script19150](struct $struct0)(dbrow)
def_dbrow $dbrow1 = null;
switch_struct ($struct0) {
    case struct_32986 :
        if (%varplayerbit_51600 < 0) {
            ~error("Invalid monster id.");
        }
        db_find(dbtable_193:col0, %varplayerbit_51600);
        $dbrow1 = db_findnext;
    case struct_50263 :
        if (%varplayerbit_56041 < 0) {
            ~error("Invalid node id.");
        }
        db_find(dbtable_294:col0, %varplayerbit_56041);
        $dbrow1 = db_findnext;
    case default :
        error("Missed plugin point for promo ID <tostring(struct_param($struct0, param_8202))>");
}
if ($dbrow1 = null) {
    ~error("Cannot find a valid dbrow.");
}
return($dbrow1);
