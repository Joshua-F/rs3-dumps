// 5240
[proc,script5240](struct $struct0)(string)
def_string $string0 = "";
def_int $int1 = ~script5241($struct0);
def_int $int2 = ~script5242($struct0);
if ($int1 > 0) {
    $string0 = "Restores <col=00ff00><tostring($int1)></col> prayer points";
} else if ($int1 < 0) {
    $string0 = "Removes <col=ff0000><tostring(abs($int1))></col> prayer points";
}
if ($int2 > 0) {
    if (string_length($string0) > 0) {
        $string0 = append($string0, ", and renews <col=00ff00><tostring($int2)> prayer points</col> over the duration");
    } else {
        $string0 = append($string0, "Renews <col=00ff00><tostring($int2)> prayer points</col> over the duration");
    }
} else if ($int2 < 0) {
    if (string_length($string0) > 0) {
        $string0 = append($string0, ", and drains <col=ff0000><tostring(abs($int2))> prayer points</col> over the duration");
    } else {
        $string0 = append($string0, "Drains <col=ff0000><tostring(abs($int2))> prayer points</col> over the duration");
    }
}
if (string_length($string0) > 0) {
    $string0 = append($string0, ".");
}
return($string0);
