// 16727
[proc,trh182_get_monster_dbrow]()(dbrow)
if (%varplayerbit_51600 < 0) {
    ~error("Invalid monster id.");
}
db_find(dbtable_193:col0, %varplayerbit_51600);
def_dbrow $dbrow0 = db_findnext;
if ($dbrow0 = null) {
    ~error("Cannot find a monster dbrow.");
}
return($dbrow0);
