// 18302
[proc,script18302](int $int0)(string)
db_find(dbtable_263:col3, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Attempting to get name for an emote with invalid id = <tostring($int0)>.");
    return(null);
}
return(db_getfield($dbrow1, dbtable_263:col0, 0));
