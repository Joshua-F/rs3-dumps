// 10742
[proc,script10742](int $int0)
if ($int0 = -1) {
    ~script10743("Select an enhancer from the list to view effects.");
    return;
}
def_obj $obj1 = enum(int, obj, enum_9456, $int0);
if ($obj1 = null) {
    ~script10743("Select an enhancer from the list to view effects.");
    return;
}
def_int $int2 = oc_param($obj1, param_4669);
if ($int2 = -1) {
    ~script10743("Select an enhancer from the list to view effects.");
    return;
}
def_int $int3 = max(1, calc(oc_param($obj1, param_4668) / 2));
def_string $string0 = oc_name($obj1);
def_string $string1 = enum(obj, string, enum_9457, $obj1);
def_int $int4 = inv_total(inv_93, obj_32708);
def_int $int5 = inv_total(inv_93, obj_32716);
if_settext($string0, interface_1554:com_16);
if ($int2 = 1 & $int4 = 0) {
    if_sethide(false, interface_1554:com_11);
    $string1 = append($string1, "<br><br>You need an unfocused damage enhancer to exchange for this.<br><br>You have:<br><tostring($int4)> unfocused damage enhancers.");
} else if ($int2 = 2 & $int5 = 0) {
    if_sethide(false, interface_1554:com_11);
    $string1 = append($string1, "<br><br>You need an unfocused reward enhancer to exchange for this.<br><br>You have:<br><tostring($int4)> unfocused reward enhancers.");
} else {
    if_sethide(true, interface_1554:com_11);
}
$string1 = append($string1, "<br><br>Enhancers from here will have half the charges of enhancers obtained from the Motherlode Maw or trading in shards.");
cc_deleteall(interface_1554:com_14);
cc_create(interface_1554:com_14, ^iftype_text, 0);
cc_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_centre);
cc_setsize(0, 0, ^setsize_minus, ^setsize_minus);
cc_settext($string1);
cc_setcolour(~script10495(12));
cc_settextfont(graphic_17813);
if ($int3 ! -1) {
    if_settext("Charges: <~tostring_spacer_local($int3)>", interface_1554:com_15);
} else {
    if_settext("-", interface_1554:com_15);
}
