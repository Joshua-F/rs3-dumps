// 126
[clientscript,friend_op](int $int0, string $string0, int $int1)
if (%varclient_1413 = 0) {
    return;
}
def_string $string1 = removetags($string0);
switch_int ($int0) {
    case 1 :
        %varclient_1650 = 1;
        %varclientstring_23 = $string1;
        ~script1558(false);
        return;
    case 2 :
        if (friend_sameplatform($int1) = true) {
            ~quickchat_open(1, $string1);
        } else {
            ~quickchat_open(3, $string1);
        }
    case 3, 4 :
        mes("That player is currently offline.");
    case 5 :
        friend_del($string1);
}
