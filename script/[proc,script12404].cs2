// 12404
[proc,script12404](int $int0)(int)
if (~script20959 = true) {
    return(0);
}
def_int $int1 = 400;
def_int $int2 = 100;
switch_int ($int0) {
    case 1 :
        if (%varplayerbit_30910 = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 2 :
        if (%varplayerbit_30922 = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 3 :
        if (%varplayerbit_30896 = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
    case 4 :
        if (%varplayerbit_30884 = 1) {
            $int2 = max(0, calc($int2 - 50));
        }
}
if (%varplayer_12121 >= 2) {
    $int2 = max(0, calc($int2 - 50));
}
$int1 = scale($int1, 100, $int2);
return($int1);
