// 13280
[proc,cheevo_obtain_progress](struct $struct0)(int)
if ($struct0 = null) {
    return(-65535);
}
if (struct_param($struct0, param_6413) = -1) {
    ~error("Struct ID is -1 - <struct_param($struct0, param_6410)> [<struct_param($struct0, param_6411)>]");
    return(-1);
}
def_int $int1 = 0;
def_int $int2 = 0;
def_enum $enum3 = null;
def_int $int4 = 0;
def_int $int5 = 0;
def_int $int6 = 0;
if (struct_param($struct0, param_6416) = 1) {
    $enum3 = struct_param($struct0, param_6426);
    if ($enum3 = null) {
        return(-1);
    }
    if (~script3983($struct0) = 1) {
        return(-2);
    }
    while ($int4 < enum_getoutputcount($enum3)) {
        $int1 = ~cheevo_obtain_progress(enum(int, struct, $enum3, $int4));
        if ($int1 = -1) {
            return(-1);
        } else if ($int1 = -2) {
            $int5 = calc($int5 + 1);
        }
        $int4 = calc($int4 + 1);
    }
    if ($int5 = enum_getoutputcount($enum3)) {
        return(-2);
    }
    return(calc(1000 - scale(calc(enum_getoutputcount($enum3) - $int5), enum_getoutputcount($enum3), 10000)));
} else {
    $int1 = ~cheevo_get_progress($struct0);
    if ($int1 = -1) {
        return(-1);
    }
    $int2 = struct_param($struct0, param_6423);
    if ($int2 = -1337) {
        $int2 = ~script13282($struct0);
    }
    if ($int1 >= $int2) {
        return(-2);
    } else if (~cheevo_requirements_met($struct0) = 0) {
        return(-1);
    } else if ($int1 > 0) {
        if (struct_param($struct0, param_6424) = 1) {
            if (struct_param($struct0, param_6425) ! -1) {
                $int6 = bitcount(calc(struct_param($struct0, param_6425) & $int1));
                return(scale($int6, struct_param($struct0, param_6423), 10000));
            } else {
                return(scale(bitcount($int1), bitcount(struct_param($struct0, param_6423)), 10000));
            }
        } else {
            return(scale($int1, struct_param($struct0, param_6423), 10000));
        }
    }
}
return(0);
