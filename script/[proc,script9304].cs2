// 9304
[proc,script9304](struct $struct0)(dbrow)
def_dbrow $dbrow1 = null;
def_enum $enum2 = null;
if ($struct0 ! null) {
    $enum2 = struct_param($struct0, param_2815);
    if ($enum2 ! null) {
        $dbrow1 = ~script18540($enum2);
    } else {
        $dbrow1 = struct_param($struct0, param_8543);
    }
}
$dbrow1 = ~script9825($struct0, $dbrow1);
return($dbrow1);
