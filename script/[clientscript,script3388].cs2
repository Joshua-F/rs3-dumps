// 3388
[clientscript,script3388](int $int0, int $int1, boolean $boolean2)
def_int $int3 = unknown_command_7008;
switch_int ($int0) {
    case 1 :
        if (autosetup_getlevel ! 1) {
            autosetup_setmin;
        } else {
            return;
        }
    case 2 :
        if (autosetup_getlevel ! 2) {
            autosetup_setlow;
        } else {
            return;
        }
    case 3 :
        if ($int3 >= 3) {
            if (autosetup_getlevel ! 3) {
                autosetup_setmedium;
            } else {
                return;
            }
        } else {
            ~script3413($int1);
        }
    case 4 :
        if ($int3 >= 4) {
            if (autosetup_getlevel ! 4) {
                autosetup_sethigh;
            } else {
                return;
            }
        } else {
            ~script3413($int1);
        }
    case 0 :
        if ($boolean2 = true) {
            if (autosetup_getlevel ! 0) {
                autosetup_setcustom;
            } else {
                return;
            }
        }
}
def_int $int4 = detailget_toolkit;
if ($int0 = 0 | $boolean2 = true) {
    ~script3387($int4, getwindowmode, ~graphics_options_reviewoptions($int4), $int1);
} else {
    ~graphics_options_rebuild($int4, getwindowmode, ~graphics_options_reviewoptions($int4), $int1);
}
