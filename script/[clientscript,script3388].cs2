// 3388
[clientscript,script3388](int $int0, int $int1)
def_int $int2 = unknown_command_7008;
switch_int ($int0) {
    case 1 :
        if (autosetup_getlevel ! 1) {
            autosetup_setmin;
        } else {
            return;
        }
    case 2 :
        if (autosetup_getlevel ! 2) {
            autosetup_setlow;
        } else {
            return;
        }
    case 3 :
        if ($int2 >= 3) {
            if (autosetup_getlevel ! 3) {
                autosetup_setmedium;
            } else {
                return;
            }
        } else {
            ~script3413($int1);
        }
    case 4 :
        if ($int2 >= 4) {
            if (autosetup_getlevel ! 4) {
                autosetup_sethigh;
            } else {
                return;
            }
        } else {
            ~script3413($int1);
        }
    case 0 :
        if (autosetup_getlevel ! 0) {
            autosetup_setcustom;
        }
}
def_int $int3 = detailget_toolkit;
if ($int0 = 0) {
    ~script3387($int3, getwindowmode, ~graphics_options_reviewoptions($int3), $int1);
} else {
    ~graphics_options_rebuild($int3, getwindowmode, ~graphics_options_reviewoptions($int3), $int1);
}
